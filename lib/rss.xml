<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title><![CDATA[MUBlog]]></title><description><![CDATA[Obsidian digital garden]]></description><link>http://github.com/dylang/node-rss</link><image><url>lib\media\favicon.png</url><title>MUBlog</title><link></link></image><generator>Webpage HTML Export plugin for Obsidian</generator><lastBuildDate>Fri, 20 Mar 2026 13:56:32 GMT</lastBuildDate><atom:link href="lib\rss.xml" rel="self" type="application/rss+xml"/><pubDate>Fri, 20 Mar 2026 13:56:29 GMT</pubDate><ttl>60</ttl><dc:creator></dc:creator><item><title><![CDATA[Lecture 1：变量与基本类型]]></title><description><![CDATA[ 
 <br><br><br>概念：const 关键词<br>
<br>概念：

<br>const 必须初始化；


<br>概念：默认状态下，const 对象仅在文件内有效

<br>编译器将在编译过程中把用到该变量的地方都替换成对应的值；
<br>也就是说，编译器会找到代码中所有用到 const 的地方，然后用其值替换；
<br>默认情况下，const 对象被设定为仅在文件内有效。当多个文件中出现了同名的 const 变量时, 其实等同于在不同文件中分别定义了独立的变量；


<br>补充：const 和 define 的区别

<br>const 行为和 #define 宏替换有点像，但 const 是类型安全的（其有数据类型检查），而宏是纯文本替换（无类型检查）；


<br>概念：const 的作用域<br>
<br>概念：

<br>C++ 中，未加 extern 修饰的 const 变量（默认状态），其作用域被编译器限定为「当前编译单元」（即单个 .cpp 文件，也叫 “翻译单元”），而非整个程序。编译器会默认给这类 const 变量加上 static 特性（注意：是编译器隐式加的，不是你手动写的 static），使其成为 “文件局部常量”，相当于这个 const 变量只属于当前 .cpp 文件，其他文件看不到；
<br>哪怕多个 .cpp 文件里定义了同名、同值的 const 变量（比如都写 const int MAX = 100;），编译器会为每个文件分别创建独立的内存对象（地址不同）。但因为编译器会把所有引用处替换成字面量值，所以表面上用起来 “好像是同一个变量”，且不会触发 “多重定义” 的链接错误；


<br>概念：让 const 在其他文件有效

<br>解决的办法是，对于 const 变量不管是声明还是定义都添加 extern 关键字，这样只需定义一次就可以了；
<br><img alt="Pasted image 20250411112948.png" src="lib\media\pasted-image-20250411112948.png">
<br>file 1.h 头文件中的声明也由 extern 做了限定，其作用是指明 bufsize 并非本文件所独有，它的定义将在别处出现；


<br><br>概念：const 的引用<br>
<br>概念：

<br>const int&amp; ref = variable; // ref是variable的常量引用


<br>分析：

<br>可以把引用绑定到 const 对象上，就像绑定到其他对象上一样，我们称之为对常量的引用；
<br>与普通引用不同的是，对常量的引用不能被用作修改它所绑定的对象；

<br>const int ci = 1024;
<br>const int &amp;r1 = ci; // 正确
<br>r1 = 42 // 错误
<br>int &amp;r2 = ci; // 错误，试图让一个非常量引用指向常量对象
<br>因为不允许直接为 ci 赋值，当然也就不能通过引用去改变 ci；
<br>因此，对 r2 的初始化是错误的，假设该初始化合法，则可以通过 r2 来改变它引用对象的值，这显然是不正确的；




<br>特点：

<br>

<br>不可修改性：通过 const 引用只能读取数据，不能修改原始对象；


<br>

<br>延长临时对象生命周期：const 引用可以绑定到临时对象（如表达式结果或函数返回值），并延长其生命周期至引用作用域结束；


<br>const int&amp; ref = 5 + 3; // 合法：临时对象生命周期延长




<br>应用：

<br>

<br>函数参数：避免拷贝，同时防止函数内部修改传入的值；


<br>void print(const std::string&amp; str)


<br>

<br>返回临时对象：返回临时对象的引用时，通常用 const 引用接收 


<br>const std::string&amp; getString()
<br>{return "Hello"; // 返回临时对象的const引用}




<br>总结：

<br><img alt="Pasted image 20250411114116.png" src="lib\media\pasted-image-20250411114116.png">


<br><br>概念：const 与指针<br>
<br>概念：

<br>指针是对象而引用不是，因此就像其他对象类型一样，允许把指针本身定为常量；
<br>常量指针 (const pointer) 必须初始化，而且一旦初始化完成，则它的值 (也就是存放在指针中的那个地址)就不能再改变了；


<br>概念：情况一 - *const

<br>把 * 放在 const 关键字之前用以说明指针是一个常量，这样的书写形式隐含着一层意味，即不变的是指针本身的值，而非指向的那个值；

<br>int errNumb = 0;
<br>int *const curErr = &amp;errNumb; // 指针curErr一直指向errNumb


<br>因为只是限定指针本身指向的地址，而没有限定此地址的值，因此可以修改其内容：

<br>*curErr = 0; // 正确




<br>概念：情况二 - const*

<br>指针指向的对象是常量，​不能通过该指针修改对象的值，但指针本身可以指向其他对象；

<br>const int* ptr;  // ptr是一个指向常量int的指针


<br>可以修改指针的指向（ptr = &amp;new_value），但不能通过指针修改指向的对象（*ptr = 10&nbsp;会报错）；


<br>概念：情况三 - 双重常量

<br>指针本身和指向的对象都是常量，既不能修改指针的指向，也不能通过指针修改对象的值； 
<br>const int* const ptr = &amp;a;  // 必须初始化


<br>总结：

<br><img alt="Pasted image 20250411114903.png" src="lib\media\pasted-image-20250411114903.png">


<br><br>概念：常量表达式 - constexpr<br>
<br>引入：

<br>在一个复杂系统中，很难分辨一个初始值到底是不是常量表达式，当然可以定义一个 const 变量并把它的初始值设为我们认为的某个常量表达式，但在实际使用时，尽管要求如此却常常发现初始值并非常量表达式的情况；
<br>可以这么说，在此种情况下，对象的定义和使用根本就是两回事儿；


<br>概念：

<br>C++11 新标准规定，允许将变量声明为 constexpr 类型以便由编译器来验证变量的值是否是一个常量表达式；
<br>声明为 constexpr 的变量一定是一个常量，而且必须用常量表达式初始化；
<br>constexpr int mf = 20; // 20是常量表达式
<br>constexpr int limit = mf + 1; // mf+1是常量表达式
<br>constexpr int sz = size(); //当size是一个constexpr函数时才是正确的声明


<br><br><br>概念：类型别名<br>
<br>概念：

<br>类型别名 (typealias) 是一个名字，它是某种类型的同义词；
<br>使用类型别名有很多好处，它让复杂的类型名字变得简单明了、易于理解和使用，还有助于程序员清楚地知道使用该类型的真实目的；


<br>方法：使用 typedef

<br>typedef double wages;
<br>typedef wages base,*p;
<br>其中含有 typedef 的声明语句定义的不再是变量，而是类型别名；


<br>举例：
<br>typedef struct ListNode 
{
    // 数据域：存储结点数据（示例用int，可替换为string/自定义类型）
    int data;
    
    // 指针域：指向“下一个结点”的指针（类型为struct ListNode*，别名后可写Node*）
    struct ListNode* next; 

    // C++结构体构造函数（初始化结点，简化创建）
    ListNode(int val) : data(val), next(nullptr) {}
} Node; // 别名Node：此后Node等价于struct ListNode
<br>
<br>方法：使用 using

<br>使用别名声明 (alias declaration) 来定义类型的别名：
<br>using SI = Sales_item
<br>SI item; // 等价于 Sales_tiem item


<br><br>概念：auto 关键字<br>
<br>概念：

<br>auto 关键字让编译器替我们去分析表达式所属的类型；
<br>和原来那些只对应一种特定类型的说明符 (比如 double) 不同，auto 让编译器通过初始值来推算变量的类型，因此 auto 定义的变量必须有初始值；


<br>使用：

<br>auto item = val1 + val2; // item初始化为 val1和 val2 相加的结果


<br>概念：auto 和引用类型<br>
<br>概念：使用引用类型

<br>使用引用其实是使用引用的对象，特别是当引用被用作初始值时, 真正参与初始化的其实是引用对象的值；
<br>因此此时编译器以引用对象的类型作为 auto 的类型；
<br>比如：int i =0,&amp;r = i;
<br>然后：auto a = r; // a 是一个整型


<br>概念：声明引用类型的 auto 

<br>情况一：auto &amp;g=ci; // g 是一个整型常量引用，绑定到 ci
<br>情况二：auto &amp;h=42; // 错误: 不能为非常量引用绑定字面值
<br>情况三：const auto &amp;j= 42; // 正确: 可以为常量引用绑定字面值


<br>概念：auto 和 const <br>
<br>

<br>auto 一般会忽略掉顶层 const，同时底层 const 则会保留下来；


<br>

<br>如果希望推断出的 auto 类型是一个顶层 const，需要明确指出 const auto f = ci;


<br>

<br>要在一条语句中定义多个变量，切记，符号 &amp; 和 * 只从属于某个声明符，而非基本数据类型的一部分，因此初始值必须是同一种类型；


<br><img alt="Pasted image 20250413015251.png" src="lib\media\pasted-image-20250413015251.png">


<br>总结<br>
<br><img alt="Pasted image 20250413015430.png" src="lib\media\pasted-image-20250413015430.png">
<br><br>概念：decltype 关键词<br>
<br>概念：

<br>decltype&nbsp;是 C++11 引入的关键字，用于推导表达式的类型，但不会计算表达式的值；
<br>它主要用于类型推断，特别是在模板编程和复杂类型声明中非常有用；


<br>举例：

<br>decltype(f()) sum = x;
<br>在这行代码当中使用 f() 的结果的类型来定义 sum 变量，但是并不实际的调用并执行 f()；


<br>概念：decltype 与引用

<br>decltype 的结果可以是引用类型：
<br>比如假设 int i = 42,&amp;r = i;
<br>则当执行 decltype(*p) c 时将会报错，因为 c 时引用类型 int&amp;，因此必须初始化；


<br>总结：区别

<br><img alt="Pasted image 20250413015840.png" src="lib\media\pasted-image-20250413015840.png">


<br>注意：

<br>decltype((variable)) (注意是双层括号) 的结果永远是引用，而 decltype(variable) 结果只有当 variable 本身就是一个引用时才是引用；


<br><br><br>概念：int* 与 int<br>
<br>概念：

<br>32 位系统：int（4 字节）、int*（4 字节，指针大小等于系统寻址位数）
<br>64 位系统：int（仍为 4 字节，C++ 标准仅规定 int 至少 16 位，主流平台保持 4 字节）、int*（8 字节，对应 64 位寻址）
<br>int* 的价值都不在于 “占用多少字节”，而在于它的指针语义，即指向内存地址、间接操作数据的能力；
<br>并且 C++ 中用 new /C 语言用 malloc 分配动态内存时，返回的必然是指针（如 int*），因为动态内存的地址需要被记录和管理，int 无法存储内存地址；


<br>代码：
<br>#include &lt;iostream&gt;
using namespace std;

int main() {
    // 动态分配一个int，返回其地址（必须用int*接收）
    int* dynamicInt = new int(20);
    cout &lt;&lt; *dynamicInt &lt;&lt; endl; // 输出20（解引用访问动态内存）
    
    // 动态分配int数组（只能用int*管理）
    int* arr = new int[5]{1,2,3,4,5};
    for (int i=0; i&lt;5; i++) {
        cout &lt;&lt; arr[i] &lt;&lt; " "; // arr[i] 等价于 *(arr+i)，通过指针访问数组
    } // 输出1 2 3 4 5
    
    // 释放动态内存（必须用指针）
    delete dynamicInt;
    delete[] arr;
    return 0;
}
<br><br>概念：int* a 与 int a[] 的区别<br>
<br>概念：

<br>这两个数组声明的核心差异，本质是静态栈数组和动态堆数组的区别：指针符号的有无，正是由它们的内存分配方式、类型本质决定的；


<br>区别：
<br><br>
<br>分析：

<br>动态数组返回的是 int* 指针，编译器只知道 “这是一个指向 int 的指针”，不知道指向的内存有多少个元素，所以 sizeof(arr) 只能得到指针本身的大小；
<br>int a[] 告诉编译器 “我要在栈上分配一块连续的内存，存储 5 个 int，变量名是 a”，数组名 a 的类型是 int[5]（数组类型），但编译器会隐式将 a 当作 const int*（常量指针） 使用，比如 a[0] 等价于 *(a+0)；


<br><br>]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-1：变量与基本类型.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 1：变量与基本类型.md</guid><pubDate>Thu, 19 Mar 2026 12:36:36 GMT</pubDate><enclosure url="lib\media\pasted-image-20250411112948.png" length="0" type="image/png"/><content:encoded>&lt;figure&gt;&lt;img src=&quot;lib\media\pasted-image-20250411112948.png&quot;&gt;&lt;/figure&gt;</content:encoded></item><item><title><![CDATA[Lecture 2：值与引用]]></title><description><![CDATA[ 
 <br><br>概念：基础概念<br>
<br>概念：形式参数

<br>在函数声明 / 定义时，写在函数名后的括号内的参数，是用来接收调用时传入数据的 “占位符”；
<br>形参是函数内部的局部变量，仅在函数被调用时分配内存，函数执行结束后立即释放，作用域仅限函数内部；


<br>概念：实际参数

<br>在函数调用时，写在函数名后的括号内的具体值 / 变量 / 表达式，是传递给形参的 “实际数据”；
<br>实参是调用函数的上下文中已存在的变量 / 常量 / 表达式，其值（或地址）会传递给形参，实参的内存不受函数调用的影响（除非传引用 / 指针修改其值）；


<br>示例：
<br>#include &lt;iostream&gt;
using namespace std;

// 形参是引用（&amp;num）：相当于实参的“别名”
void Increase(int&amp; num) { 
    num++; // 修改形参（实际是修改实参本身）
}

int main() 
{
    int val = 5;
    
    // 实参是val，传递给引用形参num
    Increase(val); 
    cout &lt;&lt; val &lt;&lt; endl; // 输出6（实参被修改）
    return 0;
}
<br><br>概念：引用传递和值传递区别<br>
<br>概念：值传递

<br>值传递的是一个值的副本，形参是实参的拷贝，改变形参的值并不会影响外部实参的值。如果是一个函数中传入值，对形参的操作不会影响实参的值；


<br>概念：引用传递

<br>概念： 

<br>被调函数的形参是「原变量的别名」（编译器层面的绑定）；虽然栈上也会存实参地址，但编译器强制让这个形参和原变量 “绑定死”—— 语法上你无法修改这个地址，操作引用就等价于直接操作原变量；
<br>对形参的操作其实就是对实参的操作，在引用传递过程中，被调函数的形式参数虽然也作为局部变量在栈中开辟了内存空间，但是这时指针指向的位置是由函数放进来的实参变量的地址。被调函数对形参的任何操作都被处理成间接寻址，即通过栈中存放的地址访问主调函数中的实参变量；
<br>正因为如此，被调函数对形参做的任何操作都影响了主调函数中的实参变量。
<br>即：引用传递传递的是引用对象的内存地址，函数对形参的操作会影响实参的值，实参的值会随着形参的值得改变而改变；


<br>总结： 

<br>引用是一个别名，它在逻辑上不是独立的，它的存在具有依附性，所以引用必须在一开始就被初始化，而且其引用的对象在其整个生命周期中是不能被改变的（自始至终只能依附于同一个变量）；




<br>概念：指针传递 

<br>概念： 

<br>指针传递参数本质上是值传递的方式，它所传递的是一个地址值；
<br>指针的本质：指针变量本质上就是一个整型数值，其数值的含义是内存地址；
<br>值传递过程中，被调函数的形式参数作为被调函数的局部变量处理，即在栈中开辟了内存空间以存放由主调函数放进来的实参的值，从而成为了实参的一个副本。值传递的特点是被调函数对形式参数的任何操作都是作为局部变量进行，不会影响主调函数的实参变量的值；


<br>总结： 

<br>指针从本质上讲就是存放变量地址的一个变量，在逻辑上是独立的，它可以被改变，包括其所指向的地址的改变和其指向的地址中所存放的数据的改变；




<br>补充：指针和变量的自增自减有什么不同？

<br>变量的自增自减是改变变量的值，指针的自增自减是改变指针的指向地址；


<br>概念：指针和引用的区别<br>
<br>概念： 

<br>指针和引用经常用于函数的参数传递，然而，指针传递参数和引用传递参数是有本质上的不同的；
<br>指针传递参数本质上是值传递的方式，它所传递的是一个地址值。值传递过程中，被调函数的形式参数作为被调函数的局部变量处理，即在栈中开辟了内存空间以存放由主调函数放进来的实参的值，从而成为了实参的一个副本。值传递的特点是被调函数对形式参数的任何操作都是作为局部变量进行，不会影响主调函数的实参变量的值；
<br>而在引用传递过程中，被调函数的形式参数虽然也作为局部变量在栈中开辟了内存空间，但是这时存放的是由主调函数放进来的实参变量的地址。被调函数对形参的任何操作都被处理成间接寻址，即通过栈中存放的地址访问主调函数中的实参变量。正因为如此，被调函数对形参做的任何操作都影响了主调函数中的实参变量；
<br>引用传递和指针传递是不同的，虽然它们都是在被调函数栈空间上的一个局部变量，但是任何对于引用参数的处理都会通过一个间接寻址的方式操作到主调函数中的相关变量。而对于指针传递的参数，如果改变被调函数中的指针地址，它将影响不到主调函数的相关变量。如果想通过指针参数传递来改变主调函数中的相关变量，那就得使用指向指针的指针，或者指针引用；


<br>概念：从编译的角度

<br>程序在编译时分别将指针和引用添加到符号表上，符号表上记录的是变量名及变量所对应地址。指针变量在符号表上对应的地址值为指针变量的地址值，而引用在符号表上对应的地址值为引用对象的地址值。符号表生成后就不会再改，因此指针可以改变其指向的对象（指针变量中的值可以改），而引用对象则不能修改；


<br>总结： 

<br>相同点： 

<br>指针和引用都是地址，指针指向一块内存，它的内容是所指内存的地址；而引用则是某块内存的别名；


<br>不同点： 

<br>（1）引用无需解引用，指针需要解引用； 
<br>（2）引用在定义时被初始化一次，之后不可变，指针可变； 
<br>（3）引用不能为空，指针可以为空； 
<br>（4）程序为指针变量分配内存区域，而引用不需要分配内存区域，所以指针自增操作是指针变量的自增，引用自增操作是变量值的自增；
<br>（5）引用没有 const，指针有 const，const 的指针不可变； 
<br>（6）“sizeof&nbsp;引用”得到的是所指向的变量 (对象)的大小，而“sizeof&nbsp;指针”得到的是指针本身的大小；
<br>（7）引用是类型安全的，而指针不是，引用比指针多了类型检查；




<br><br><br><br>代码<br>#include &lt;iostream&gt;


void Fun1(int number) 
{
    std::cout &lt;&lt; &amp;number &lt;&lt; std::endl;
    std::cout &lt;&lt; number &lt;&lt; std::endl; 
    number++;
}

void Fun2(int&amp; number)
{
    std::cout &lt;&lt; number &lt;&lt; std::endl;
    std::cout &lt;&lt; &amp;number &lt;&lt; std::endl;
    number++;
}

void Fun3(int* number)
{
    std::cout &lt;&lt; number &lt;&lt; std::endl;
    std::cout &lt;&lt; &amp;number &lt;&lt; std::endl;
    std::cout &lt;&lt; *number &lt;&lt; std::endl;

    *number = *number + 1;
    std::cout &lt;&lt; number &lt;&lt; std::endl;
    std::cout &lt;&lt; &amp;number &lt;&lt; std::endl;
    std::cout &lt;&lt; *number &lt;&lt; std::endl;
}

int main()
{
    int number = 10;
    std::cout &lt;&lt; "Fun1():" &lt;&lt; std::endl;
    Fun1(number);

    std::cout &lt;&lt; "Fun2():" &lt;&lt; std::endl;
    Fun2(number);


    std::cout &lt;&lt; "Fun3():" &lt;&lt; std::endl;
    Fun3(&amp;number);
}
<br>
<br>运行截图：

<br><img alt="Pasted image 20250111143310.png" src="lib\media\pasted-image-20250111143310.png">


<br>分析： 

<br>由运行结果可知：
<br>

<br>值传递时，输出 &amp;number 得到的是局部参数在栈上的地址，输出 number 得到的是数值；


<br>

<br>引用传递时，输出 &amp;number 得到的是 main() 函数执行 int number 后、在栈上分配的地址；


<br>

<br>指针传递时，直接输出 number 得到的是内存地址，即 main() 函数执行 int number 后、在栈上分配的地址。而输出 &amp;number 表示对 000000769F5AFA54 进行寻址，得到其在栈上地址；




<br><br>概念：struct 示例<br>
<br>代码：
<br>#include &lt;iostream&gt;
using namespace std;

// 定义值类型结构体（包含大数组，拷贝成本极高）
struct List {
    int data[1000];
    int len;
    // 构造函数：初始化len为0
    List() : len(0) {}
};

// 1. 指针传递：形参是List*（存储原结构体的地址副本）
void ModifyListByPtr(List* listPtr) {
    // 必须用-&gt;操作符访问成员（指针解引用+成员访问）
    if (listPtr != nullptr) { // 必须判空，否则崩溃
        listPtr-&gt;len = 10;    // 修改原结构体的len
        listPtr-&gt;data[0] = 100; // 修改原结构体的数组
        // 指针可重指向其他List对象（仅修改形参指针，不影响主调函数）
        List temp;
        listPtr = &amp;temp; 
        listPtr-&gt;len = 20; // 仅修改temp，和原结构体无关
    }
}

// 2. 引用传递：形参是List&amp;（原结构体的别名）
void ModifyListByRef(List&amp; listRef) {
    // 直接用.操作符访问成员（引用是别名，等价于操作原结构体）
    listRef.len = 20;       // 修改原结构体的len
    listRef.data[0] = 200;  // 修改原结构体的数组
    // 错误：引用无法重绑定到其他对象（编译器强制约束）
    // List temp;
    // listRef = temp; // 这不是重绑定，是把temp的值拷贝给原结构体！
}

int main() {
    List myList; // 定义原结构体（值类型，存储在栈上）
    cout &lt;&lt; "修改前：len=" &lt;&lt; myList.len &lt;&lt; ", data[0]=" &lt;&lt; myList.data[0] &lt;&lt; endl;

    // 指针传递调用：必须传地址（&amp;myList）
    ModifyListByPtr(&amp;myList);
    cout &lt;&lt; "指针传递后：len=" &lt;&lt; myList.len &lt;&lt; ", data[0]=" &lt;&lt; myList.data[0] &lt;&lt; endl; // len=10, data[0]=100

    // 引用传递调用：直接传变量名（myList）
    ModifyListByRef(myList);
    cout &lt;&lt; "引用传递后：len=" &lt;&lt; myList.len &lt;&lt; ", data[0]=" &lt;&lt; myList.data[0] &lt;&lt; endl; // len=20, data[0]=200

    return 0;
}
<br>
<br>总结：
<br><br><br><br>概念：右值基础概念<br>
<br>概念：为什么需要右值引用

<br>右值引用主要用于解决不必要的 Copy，当程序在装载对象的时候经常会制造出临时对象；
<br>而这是如果时 Copy 行为，因为要将这个对象完整复制一份，因此更加消耗性能。右值引用就是用于解决不必要的拷贝；
<br>即当右手边是右值时，左手边可以直接引用右边的值，而无需重新分配内存；


<br>概念：左值与右值

<br>左值与左值引用：

<br>左值就是变量，可以放在等于号的左右两边；
<br>int a = 5;
<br>int &amp;ref_a = a; // 左值引用指向左值，编译通过
<br>int &amp;ref_a = 5; // 左值引用指向了右值，会编译失败
<br>引用是变量的别名，由于右值没有地址，没法被修改，所以左值引用无法指向右值
<br>但是，const 左值引用是可以指向右值：

<br>const int &amp;ref_a = 5;  // 编译通过


<br>const 左值引用不会修改指向值，因此可以指向右值，这也是为什么要使用 const &amp; 作为函数参数的原因之一，如 std::vector 的 push_back： 

<br>void push_back (const value_type&amp; val);


<br>如果没有 const，vec.push_back(5) 这样的代码就无法编译通过了；


<br>右值与右值引用： 

<br>右值就是内容本身，只可以放在等于号的右边；
<br>比如：临时对象 string() = "World"; 此代码创建了一个 String 类型的临时对象；




<br>举例： 

<br>在下面的例子中，foo 返回的内容为右值，无法直接对其取地址；
<br>在 int x = foo() 的时候，直接将“5”这个 int 类型的对象进行了复制，然后赋值到了 x 变量上；


<br>#include &lt;iostream&gt;
using namespace std;

int main() 
{
    int&amp;&amp; s1 = 123; // 核心代码
    cout &lt;&lt; "s1的值: " &lt;&lt; s1 &lt;&lt; endl;       // 输出：123
    cout &lt;&lt; "s1的地址: " &lt;&lt; &amp;s1 &lt;&lt; endl;    // 输出：s1有内存地址（证明是对象）
    s1 = 456;                               // 可以修改（右值引用是可修改的）
    cout &lt;&lt; "修改后s1的值: " &lt;&lt; s1 &lt;&lt; endl; // 输出：456
    return 0;
}
<br>
<br>分析：

<br>123 是纯右值（没有内存地址），编译器会先创建一个临时的 int 对象（比如命名为 temp），值为 123，这个临时对象是 “将亡值（xvalue）”（属于右值的一种）
<br>右值引用 s1 绑定到这个临时对象 temp 上。根据 C++  标准规则：绑定到右值引用的临时对象，其生命周期会被延长至与引用变量（s1）的生命周期一致，直到 s 1 出作用域销毁；
<br>此时 s1 不再是 “纯右值”，而是一个有名字的右值引用（本质是左值！因为它有内存地址、可被取址），可以被修改（如 s1=456）


<br>补充：

<br>对于 int 这类内置类型（char、float、bool 等），右值引用没有 “性能收益”；
<br>因为 int 的大小只有 4 字节，“移动” 和 “拷贝” 都是直接复制值，没有资源（如堆内存、文件句柄）可转移。移动语义的价值体现在带资源的自定义类型（如 std::string、std::vector）上；


<br><br>概念：右值引用的使用<br>
<br>概念： 

<br>右值引用的意义，就是右值引用可以使我们对右值取地址；
<br>右值引用专门为右值而生，可以指向右值，不能指向左值；


<br>语法： 

<br>&amp;&amp;


<br>注意： 

<br>执行指针的浅拷贝，上一根指针不可再被使用；


<br>代码：
<br>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;string&gt;

using namespace std;

int main() 
{
    vector&lt;string&gt; vec;
    vec.reserve(3); // 预分配空间，避免扩容

    // 1. 拷贝插入：创建临时string，拷贝到vec
    string s1 = "copy";
    vec.push_back(s1);
    cout &lt;&lt; "s1（拷贝后）: " &lt;&lt; s1 &lt;&lt; endl; // 输出：copy

    // 2. 移动插入：将临时string（右值）移动到vec
    string s2 = "move";
    // 原本的 s2 是左值，但是通过 std::move 将其变成右值，并且push_back这些STL标准库的容器都提供了移动语义的接口，因此直接将 move(s2) 得到的右值引用的所有权移动到 vec 身上
    // 因此 s2 就失去了拥有权
    vec.push_back(move(s2));
    cout &lt;&lt; "s2（移动后）: " &lt;&lt; s2 &lt;&lt; endl; // 输出：空

    // 3. emplace_back 直接构造（本质也是利用移动/构造优化）
    vec.emplace_back("emplace"); // 直接在vec的内存中构造string，无拷贝/移动

    cout &lt;&lt; "vec的元素: ";
    for (const auto&amp; s : vec) cout &lt;&lt; s &lt;&lt; " "; // 输出：copy move emplace
    cout &lt;&lt; endl;

    return 0;
}
<br>分析：右值引用的应用<br>
<br>概念：

<br>STL 当中的 insert() 函数；


<br>分析： 

<br>当使用 insert(&amp;x) 时，其本质上是进行的赋值；
<br>当使用 insert(&amp;&amp;x) 时，其会有两种可能性：

<br>可能性一：传入右值

<br>此时将会调用执行传进来的对象“x”对应的 ctor 函数（右值引用的版本），通过构造函数获得对象浅拷贝的结果；
<br>本质上就是直接把对象的指针直接偷过来，之后不可再使用其；


<br>可能性二：传入左值

<br>传入的值为左值时，执行第一个重载






<br><br>概念：右值引用与 struct<br>
<br>示例：
<br>#include &lt;iostream&gt;
using namespace std;

struct List 
{
    int data[1000];
    int len;
    List() : len(0) {
        cout &lt;&lt; "默认构造：分配栈上数组" &lt;&lt; endl;
    }
    
    // 默认拷贝构造（编译器自动生成）：逐字节拷贝data[1000] + len
    List(const List&amp; other) 
    {
        // 手动实现默认拷贝逻辑，便于观察
        for (int i = 0; i &lt; 1000; i++) 
        {
            data[i] = other.data[i];
        }
        
        len = other.len;
        cout &lt;&lt; "拷贝构造：拷贝1000个int（开销大）" &lt;&lt; endl;
    }
};

int main() 
{
    // 1. 创建临时List对象（右值）
    List tempList;
    tempList.data[0] = 100;
    tempList.len = 1;

    // 2. 右值引用绑定“即将销毁的右值”（用std::move将左值转为右值）
    List&amp;&amp; rvalueRef = std::move(tempList); 
    cout &lt;&lt; "右值引用访问：" &lt;&lt; rvalueRef.data[0] &lt;&lt; endl; // 输出100

    // 3. 用右值引用初始化新对象：依然触发拷贝构造（无移动语义）
    List newList = std::move(rvalueRef); 
    return 0;
}
<br>
<br>分析：

<br>data[1000] 是栈上数组，栈内存的生命周期由作用域决定（随函数 / 对象销毁自动释放），无法转移所有权。即使绑定了右值引用，创建新对象时依然要完整拷贝 1000 个 int，移动语义在此场景下 “无效”；
<br>若未自定义移动构造函数，编译器会优先调用拷贝构造函数处理右值（因为拷贝构造的参数 const T&amp; 可以绑定右值），导致即使是临时对象，也会触发高开销的拷贝；


<br>示例：改造 List
<br>struct List 
{
    int* data; // 堆上动态数组
    int len;
    int capacity; // 数组容量

    // 1. 默认构造：分配堆上数组
    List(int cap = 1000) : len(0), capacity(cap) {
        data = new int[capacity]; // 堆上分配1000个int
        cout &lt;&lt; "默认构造：分配堆上数组（地址：" &lt;&lt; (void*)data &lt;&lt; "）" &lt;&lt; endl;
    }

    // 2. 拷贝构造：深拷贝（完整复制堆上数组，开销大）
    List(const List&amp; other) 
    {
        len = other.len;
        capacity = other.capacity;
        data = new int[capacity]; // 重新分配堆内存
        for (int i = 0; i &lt; len; i++) {
            data[i] = other.data[i]; // 逐元素拷贝
        }
        cout &lt;&lt; "拷贝构造：深拷贝堆数组（地址：" &lt;&lt; (void*)data &lt;&lt; "）" &lt;&lt; endl;
    }

    // 3. 析构函数：释放堆内存（避免泄漏）
    ~List() {
        if (data != nullptr) {
            delete[] data;
            cout &lt;&lt; "析构：释放堆数组（地址：" &lt;&lt; (void*)data &lt;&lt; "）" &lt;&lt; endl;
            data = nullptr;
        }
    }

    // 4. 移动构造函数（核心：右值引用参数 + 转移资源）
    List(List&amp;&amp; other) noexcept // noexcept：告诉编译器无异常，容器会优先调用 
    { 
        // 转移资源所有权（仅拷贝指针，无数组拷贝）
        data = other.data;
        len = other.len;
        capacity = other.capacity;

        // 置空源对象的指针（避免析构时重复释放）
        other.data = nullptr;
        other.len = 0;
        other.capacity = 0;

        cout &lt;&lt; "移动构造：转移堆数组（地址：" &lt;&lt; (void*)data &lt;&lt; "）" &lt;&lt; endl;
    }

    // 5. 移动赋值运算符（处理a = std::move(b)场景）
    List&amp; operator=(List&amp;&amp; other) noexcept {
        if (this == &amp;other) 
        { // 防止自赋值
            return *this;
        }

        // 释放当前对象的堆资源
        if (data != nullptr) 
        {
            delete[] data;
        }

        // 转移源对象的资源
        data = other.data;
        len = other.len;
        capacity = other.capacity;

        // 置空源对象
        other.data = nullptr;
        other.len = 0;
        other.capacity = 0;

        cout &lt;&lt; "移动赋值：转移堆数组（地址：" &lt;&lt; (void*)data &lt;&lt; "）" &lt;&lt; endl;
        return *this;
    }

    // 禁用拷贝赋值（可选，突出移动语义）
    List&amp; operator=(const List&amp; other) = delete;
};

int main() {
    // 1. 创建临时List对象（右值）
    List tempList;
    tempList.data[0] = 100;
    tempList.len = 1;

    // 2. 用std::move将左值转为右值，触发移动构造
    List newList = std::move(tempList); 

    // 3. 验证：tempList的资源已被转移（指针为空）
    cout &lt;&lt; "tempList.data是否为空：" &lt;&lt; (tempList.data == nullptr ? "是" : "否") &lt;&lt; endl;
    cout &lt;&lt; "newList.data[0]：" &lt;&lt; newList.data[0] &lt;&lt; endl;

    return 0;
}
<br><br><br>概念：左值和右值<br>
<br>总结：
<br><br><br>概念：移动语义<br>
<br>引入：

<br>C++11 之前，对象的 “复制” 只有拷贝语义：当我们用一个对象初始化另一个对象（如 List a = b;），或给对象赋值（如 a = b;）时，编译器会调用拷贝构造函数 / 拷贝赋值运算符，对对象的所有资源（尤其是堆上资源，如动态数组、文件句柄、内存缓冲区）做 “深拷贝”—— 即重新分配一块内存，再把原对象的内容逐字节复制过去；
<br>临时对象的资源 “拷贝完就扔”，完全没必要：移动语义的核心目标就是解决这种 “无意义拷贝” 的性能问题；


<br>概念： 

<br>将一个对象（通常是生命周期极短的右值）的「资源所有权」直接转移给另一个对象，而非复制资源本身；
<br>当传入的值为左值，但是你明确的知道此左值再未来不会被再进行操作，此时就可以在传入参数时用 std::move() 将其指明为右值进行传入，从而进行浅拷贝；
<br>std::move（函数，&lt;utility&gt;）是将左值转为右值引用；std::move（算法，&lt;algorithm&gt;）是将一个范围的元素移动到另一个范围，避免拷贝元素；


<br>补充：

<br>很多人误以为 std::move 会 “移动资源”，这是典型误区；
<br>std::move 的唯一作用是：将传入的参数（无论左值 / 右值）强制转换成对应的右值引用类型，它本身不执行任何内存操作、不转移任何资源
<br>
  // 模板版本（简化）
template &lt;typename T&gt;
typename remove_reference&lt;T&gt;::type&amp;&amp; move(T&amp;&amp; t) noexcept {
    // 核心：静态转换为右值引用
    return static_cast&lt;typename remove_reference&lt;T&gt;::type&amp;&amp;&gt;(t);
}


<br>它的本质是一个类型转换函数，没有任何逻辑（如内存拷贝、资源转移），只是改变了参数的 “值类别”（左值 → 右值引用）


<br>语法： 

<br>std::move()


<br>举例：
<br>int a = 5; // a是个左值
int &amp;ref_a_left = a; // 左值引用指向左值
int &amp;&amp;ref_a_right = std::move(a); // 通过std::move将左值转化为右值，可以被右值引用指向
 
cout &lt;&lt; a; // 打印结果：5
<br><br>概念：移动语义在 STL 中的使用<br>
<br>概念：

<br>移动语义（基于右值引用 T&amp;&amp;）的核心是资源转移而非拷贝，STL（标准模板库）中的容器、算法、工具函数都广泛实现了移动语义，目的是避免不必要的深拷贝，大幅提升性能；
<br>所有 STL 容器（如 vector、string、map 等）都默认实现了移动构造函数和移动赋值运算符；


<br>示例：STL 容器的移动构造与移动赋值
<br>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;string&gt;

using namespace std;

void printVector(const vector&lt;string&gt;&amp; v, const string&amp; name) {
    cout &lt;&lt; name &lt;&lt; " size: " &lt;&lt; v.size() &lt;&lt; ", data: ";
    for (const auto&amp; s : v) cout &lt;&lt; s &lt;&lt; " ";
    cout &lt;&lt; endl;
}

int main() {
    // 1. 构造一个包含大量数据的vector
    vector&lt;string&gt; vec1 = {"apple", "banana", "cherry", "date"};
    printVector(vec1, "vec1（移动前）");

    // 2. 移动构造 vec2：将vec1的资源的所有权转移给vec2
    vector&lt;string&gt; vec2(std::move(vec1));
    printVector(vec2, "vec2（移动构造后）");
    printVector(vec1, "vec1（被移动后）"); // 移动后vec1处于"有效但未指定"状态（通常为空）

    // 3. 移动赋值vec3
    vector&lt;string&gt; vec3;
    vec3 = move(vec2);
    printVector(vec3, "vec3（移动赋值后）");
    printVector(vec2, "vec2（移动后）");

    return 0;
}

// 结果：
vec1（移动前） size: 4, data: apple banana cherry date 
vec2（移动构造后） size: 4, data: apple banana cherry date 
vec1（移动后） size: 0, data: 
vec3（移动赋值后） size: 4, data: apple banana cherry date 
vec2（移动后） size: 0, data: 
<br>
<br>分析：

<br>vector&lt;string&gt; vec2(move(vec1))：调用 vector 的移动构造函数，仅转移 vec1 的内存指针（而非拷贝字符串数据），时间复杂度 
<br>移动后原对象（vec1）的状态：C++ 标准规定其处于 “有效但未指定” 状态（通常为空），不能再依赖其原有数据，但可以安全地调用析构函数或重新赋值； 


<br>示例：函数返回 STL 容器时的移动语义
<br>#include &lt;iostream&gt;
#include &lt;vector&gt;

using namespace std;

// 函数返回一个大vector
vector&lt;int&gt; createBigVector() {
    vector&lt;int&gt; vec(1000000, 1); // 100万个元素
    cout &lt;&lt; "函数内vec的地址: " &lt;&lt; &amp;vec[0] &lt;&lt; endl;
    return move(vec); // 显式移动（也可省略，编译器会自动优化）
}

int main() 
{
    vector&lt;int&gt; result = createBigVector();
    cout &lt;&lt; "main中result的地址: " &lt;&lt; &amp;result[0] &lt;&lt; endl;
    cout &lt;&lt; "result大小: " &lt;&lt; result.size() &lt;&lt; endl;
    return 0;
}

// 结果：
函数内vec的地址: 0x55f8a7a2a010
main中result的地址: 0x55f8a7a2a010
result大小: 1000000
<br>
<br>分析：

<br>返回的 vec 是局部对象（右值），编译器会自动调用移动构造函数，将 vec 的内存缓冲区直接转移给 result，而非拷贝 100 万个 int；


<br>示例：移动元素
<br>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;string&gt;
#include &lt;algorithm&gt; // std::move算法的头文件

using namespace std;

int main() {
    vector&lt;string&gt; src = {"hello", "world", "move", "semantics"};
    vector&lt;string&gt; dest(src.size()); // 预分配空间

    // 使用std::move算法，将src的元素移动到dest
    move(src.begin(), src.end(), dest.begin());

    cout &lt;&lt; "dest的元素: ";
    for (const auto&amp; s : dest) cout &lt;&lt; s &lt;&lt; " "; // 输出：hello world move semantics
    cout &lt;&lt; endl;

    cout &lt;&lt; "src的元素（移动后）: ";
    for (const auto&amp; s : src) cout &lt;&lt; "[" &lt;&lt; s &lt;&lt; "] "; 
    // 输出：[] [] [] []（string移动后变为空）
    cout &lt;&lt; endl;

    return 0;
}
<br><br>概念：转交<br>
<br>概念：不完美转交

<br>当一个函数在参数中接受右值时，会将此右值当成变量成为另一个函数的参数；
<br>传入到此函数当中的另一个函数时，其作为参数传入进来的值在使用时就又变为了左值。此时就是不完美转交；


<br>概念：完美转交

<br>std::forward&lt;T1&gt;(t1)


<br>举例：
<br>template&lt;typename T1,typename T2&gt;
void functionA(T1&amp;&amp; t1,T2&amp;&amp; t2)
{
	functionB(std::forward&lt;T1&gt;(t1),std::forward&lt;T2&gt;(t2));
}
]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-2：值与引用.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 2：值与引用.md</guid><pubDate>Wed, 18 Mar 2026 11:29:21 GMT</pubDate><enclosure url="lib\media\pasted-image-20250111143310.png" length="0" type="image/png"/><content:encoded>&lt;figure&gt;&lt;img src=&quot;lib\media\pasted-image-20250111143310.png&quot;&gt;&lt;/figure&gt;</content:encoded></item><item><title><![CDATA[Lecture 3：虚函数与纯虚函数]]></title><description><![CDATA[ 
 <br><br><br>概念：虚函数<br>
<br>概念： 

<br>虚函数是在基类中使用关键字 virtual 声明的成员函数，它允许派生类对其进行重写（Override），实现运行时多态；
<br>当通过基类指针或引用调用虚函数时，实际调用的是对象类型对应的派生类中的函数，这个过程称为动态绑定（Dynamic Binding）或晚绑定（Late Binding）；


<br>实现： 

<br>虚函数的实现原理基于虚函数表（Virtual Table，简称 VTable）；
<br>每个使用虚函数的类都有一个虚函数表，该表是一个函数指针数组，存储了指向类的虚函数的指针；
<br>类的每个实例都包含一个指向其虚函数表的指针（vptr），通过这个指针可以找到并调用正确的虚函数实现；
<br>当派生类覆盖（重写）基类的虚函数时，派生类的虚函数表中相应位置的函数指针会被更新为指向派生类中的函数；
<br>如果派生类没有重写虚函数，则派生类的虚函数表中会保留指向基类虚函数的指针；


<br>举例：
<br>#include &lt;iostream&gt;
using namespace std;

class Base 
{
public:
    virtual void show() 
    {
        cout &lt;&lt; "Base class show" &lt;&lt; endl;
    }
};

class Derived : public Base 
{
public:
    void show() override 
    {
        cout &lt;&lt; "Derived class show" &lt;&lt; endl;
    }
};

int main() {
    Base* b = new Derived();
    b-&gt;show(); // 输出：Derived class show
    delete b;
    return 0;
}
<br><br>概念：虚函数的重写<br>
<br>概念： 

<br>虚函数重写指的是派生类中提供一个函数版本，该版本与基类中具有相同名称、相同返回类型和相同参数列表的虚函数相匹配。通过这种方式，派生类可以提供自己特定的实现，替换或扩展基类的行为；


<br>补充： 

<br>（1）基类函数必须是虚函数 

<br>只有虚函数可以被重写；
<br>如果基类中的函数不是虚函数，派生类中相同签名的函数会隐藏（而非重写）基类中的函数；


<br>（2）访问权限可以不同 

<br>虚函数在派生类中的访问级别（public、protected、private）可以与基类中的不同；


<br>（3）析构函数应该是虚的

<br>如果一个类有可能被继承，并且通过基类指针来删除派生类对象，那么基类的析构函数应该是虚的；
<br>这确保了通过基类指针删除派生类对象时，能够正确地调用派生类的析构函数；


<br>（4）构造函数不能是虚函数

<br>在 C++ 中，构造函数不能被声明为虚函数；
<br>因为构造函数是用来创建对象的，而虚函数的调用需要通过对象的虚函数表，这在对象构造阶段还未完全建立； 


<br>（5）使用&nbsp;final&nbsp;关键字防止进一步重写

<br>在某些情况下，你可能希望禁止进一步重写某个虚函数；
<br>C++11 引入了 final 关键字，可以用来阻止派生类重写特定的虚函数；




<br>举例：
<br>#include &lt;iostream&gt;

class Base {
public:
    virtual void print() const 
    {
        std::cout &lt;&lt; "Base class print function" &lt;&lt; std::endl;
    }
    
    virtual ~Base() {} // 虚析构函数
};

class Derived : public Base 
{
public:
	// /使用 override 确保正确重写
    void print() const override 
    { 
        std::cout &lt;&lt; "Derived class print function" &lt;&lt; std::endl;
    }
};

int main() {
    Base* b = new Derived();
    b-&gt;print(); // 输出：Derived class print function
    delete b; // 正确调用派生类析构函数
    return 0;
}
<br><br>概念：虚函数表<br>
<br>概念： 

<br>当涉及到继承时，虚函数表（vtable）的处理方式会稍微复杂一些，但关键点在于每个类都有自己的虚函数表，而不是只有一个；
<br>这意味着，如果有派生类继承自基类，并且这些类中包含虚函数，那么每个类将拥有各自独立的虚函数表；
<br>当调用虚函数时，编译器不直接调用函数，而是生成如下逻辑：


<br>规则： 

<br>（1）基类

<br>在基类中，编译器会为其创建一个虚函数表，这个表包含了基类中所有虚函数的地址；
<br>如果派生类没有覆盖（重写）这些虚函数，派生类对象的虚函数表会复制基类虚函数表中相应的条目；


<br>（2）派生类 

<br>当派生类覆盖（重写）基类中的虚函数时，派生类的虚函数表中对应位置的函数指针会被更新为指向派生类中的函数实现；
<br>如果派生类引入了新的虚函数，这些新的虚函数也会被加入到派生类的虚函数表中；


<br>（3）多重继承 

<br>在多重继承的情况下，每个基类都会有自己的虚函数表；
<br>派生类对象会包含多个虚函数表指针，每个指针指向对应基类的虚函数表。如果派生类覆盖了某个基类的虚函数，那么相关基类虚函数表中的条目会被更新为指向派生类中的实现；




<br>代码：虚函数表项分析<br>
<br>示例：
<br>class Base {
public:
    virtual void func1 () { /* 实现 */ }
    virtual void func2 () { /* 实现 */ }
};

class Derived : public Base {
public:
    void func1 () override { /* 新实现 */ }
    virtual void func3 () { /* 新虚函数 */ }
};
<br>
<br>分析： 

<br>Base&nbsp;类有自己的虚函数表，包含&nbsp;func1&nbsp;和&nbsp;func2。
<br>Derived&nbsp;类有自己的虚函数表，其中&nbsp;func1&nbsp;的条目会被更新为指向&nbsp;Derived::func1，func2&nbsp;保持不变（因为它没有被 Derived 重写），并且会添加一个新的条目指向&nbsp;func3


<br><br><br>概念：纯虚函数<br>
<br>概念： 

<br>纯虚函数是在基类中声明但不实现的虚函数，其声明方式是在函数声明的结尾处添加&nbsp;= 0
<br>类中如果包含至少一个纯虚函数，则该类成为抽象类（Abstract Class），不能实例化对象；
<br>纯虚函数的主要作用是定义接口规范，强制要求派生类必须实现这些函数，从而实现接口的统一和标准化；


<br>分析： 

<br>定义一个函数为虚函数，不代表函数为不被实现的函数，定义他为虚函数是为了允许用基类的指针来调用子类的这个函数；
<br>定义一个函数为纯虚函数，才代表函数没有被实现。定义纯虚函数是为了实现一个接口，起到一个规范的作用，规范继承这个类的程序员必须实现这个函数；
<br>纯虚函数的意义，让所有的类对象（主要是派生类对象）都可以执行纯虚函数的动作，但类无法为纯虚函数提供一个合理的默认实现。所以类纯虚函数的声明就是在告诉子类的设计者，"你必须提供一个纯虚函数的实现，但我不知道你会怎样实现它"；


<br>示例：
<br>#include &lt;iostream&gt;
using namespace std;

class Shape 
{
public:
    virtual void draw() = 0; // 纯虚函数
};

class Circle : public Shape 
{
public:
    void draw() override {
        cout &lt;&lt; "Drawing a circle" &lt;&lt; endl;
    }
};

int main() {
    Shape* shape = new Circle();
    shape-&gt;draw(); // 输出：Drawing a circle
    delete shape;
    return 0;
}
<br><br>概念：抽象类<br>
<br>概念： 

<br>抽象类是一种特殊的类，它是为了抽象和设计的目的为建立的，它处于继承层次结构的较上层；


<br>定义：

<br>称带有纯虚函数的类为抽象类；


<br>作用：

<br>抽象类的主要作用是将有关的操作作为结果接口组织在一个继承层次结构中，由它来为派生类提供一个公共的根，派生类将具体实现在其基类中作为接口的操作；
<br>所以派生类实际上刻画了一组子类的操作接口的通用语义，这些语义也传给子类，子类可以具体实现这些语义，也可以再将这些语义传给自己的子类；


<br>注意：

<br>抽象类只能作为基类来使用，其纯虚函数的实现由派生类给出；
<br>如果派生类中没有重新定义纯虚函数，而只是继承基类的纯虚函数，则这个派生类仍然还是一个抽象类；
<br>如果派生类中给出了基类纯虚函数的实现，则该派生类就不再是抽象类了，它是一个可以建立对象的具体的类；


<br><br><br>概念：协变<br>
<br>概念： 

<br>C++ 中，协变（covariant）是指派生类（子类）中的返回类型可以是基类（父类）中返回类型的子类型；
<br>换句话说，如果一个虚函数在基类中返回的是基类类型的指针或引用，那么派生类可以重写该虚函数并返回基类类型的子类类型的指针或引用；
<br>协变在 C++ 中是通过使用返回类型协变（return type covariance）来实现的。返回类型协变是指派生类中重写的虚函数可以具有比基类更具体的返回类型；
<br>这种协变的能力使得在使用多态时更加灵活，可以根据具体的派生类返回不同的子类型，而不需要进行显式的类型转换；


<br>规则：

<br>基类中的函数必须是虚函数（使用 virtual 关键字声明）；
<br>派生类中重写的函数必须具有相同的函数签名（函数名、参数列表和常量性）；
<br>派生类中重写的函数的返回类型必须是基类函数返回类型的子类型；


<br>分析：

<br>协变成立的前提：子类指针 / 引用可以隐式转换为基类指针 / 引用，且函数的参数列表、const/volatile 属性完全匹配（仅返回值协变）
<br>vtable 中仍存储子类重写函数的指针，调用时直接执行子类函数；编译器会在函数返回后，自动将子类指针 / 引用转换为基类指针 / 引用（对用户透明） 


<br><br>代码：协变示例<br>
<br>举例：
<br>class Shape
{
public:
    virtual Shape* clone() const = 0;
}

class Circle:public Shape
{
public:
    Circle* clone() const;
}
<br>
<br>分析： 

<br>重写的派生类函数被声明为返回一个 Circle* 而不是继续 Shape*，因为语义上，Circle 是一个 Shape；
<br>直接操纵派生类类型而不是通过基类接口来操纵它们时，协变返回类型的优势便会体现出来：
<br>如果不使用协变返回类型机制，或者不存在协变返回类型机制，Circle::clone 将不得不精确地匹配 Shape::clone 的返回类型，从而返回一个 Shape*，我们就必须被迫将结果转换为 Circle*； 


]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-3：虚函数与纯虚函数.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 3：虚函数与纯虚函数.md</guid><pubDate>Thu, 19 Mar 2026 11:25:23 GMT</pubDate></item><item><title><![CDATA[Lecture 4：内联函数]]></title><description><![CDATA[ 
 <br><br>概念：内联函数<br>
<br>语法： 

<br>在函数定义前加上关键字 inline；


<br>概念： 

<br>关键字&nbsp;inline&nbsp;建议编译器使用函数定义中的代码替换对该函数的每次调用。理论上，使用内联函数可以加速程序运行，因为它们消除了与函数调用关联的开销；
<br>调用函数需要将返回地址推送到堆栈、将参数推送到堆栈、跳转到函数体，然后在函数完成时执行返回指令。通过内联函数可以消除此过程。相对于未内联扩展的函数，编译器还有其他机会来优化内联扩展的函数；
<br>类声明的主体中定义的函数是隐式内联函数。


<br>目的： 

<br>（1）代替部分 #define 宏定义

<br>​​宏是预处理指令，在预处理的时候把所有的宏名用宏体来替换；
<br>内联函数是函数，在编译阶段把所有调用内联函数的地方把内联函数插入；
<br>因此，宏没有类型检查，无论对还是错都是直接替换；而内联函数在编译时进行安全检查；​
<br>宏的编写有很多限制，例如只能写一行，不能使用 return 控制流程等；
<br>对于 C++ 而言，使用宏代码还有另一种缺点：无法操作类的私有数据成员；
<br>使用内联函数可以解决上述问题，内联函数提供了安全检查，并且编写相比于宏函数更加方便；


<br>（2）代替普通函数、提升程序运行效率

<br>函数是一个可以重复使用的代码块，CPU 会一条一条地挨着执行其中的代码。CPU 在执行主调函数代码时如果遇到了被调函数，当前程序的执行流转向执行被调函数的代码；
<br>被调函数执行完毕后再返回到主调函数，主调函数根据刚才的状态继续往下执行；
<br>一个 C/C++ 程序的执行过程可以认为是多个函数之间的相互调用过程，它们形成了一个或简单或复杂的调用链条，这个链条的起点是 main()，终点也是 main()。当 main() 调用完了所有的函数，它会返回一个值来结束自己的生命，从而结束整个程序；
<br>而函数调用是有时间和空间开销的。程序在执行一个函数之前需要做一些准备工作，要将实参、局部变量、返回地址以及若干寄存器都压入栈中，然后才能执行函数体中的代码；
<br>函数体中的代码执行完毕后还要清理现场，将之前压入栈中的数据都出栈，才能接着执行函数调用位置以后的代码；假如频繁大量的使用就会造成因栈空间不足所造成的程式出错的问题，函数的死循环递归调用的最终结果就是导致栈内存空间枯竭。
<br>如果函数体代码比较多，需要较长的执行时间，那么函数调用机制占用的时间可以忽略；
<br>如果函数只有一两条语句，那么大部分的时间都会花费在函数调用机制上，这种时间开销就就不容忽视，为了消除函数调用的时空开销，C++ 提供一种提高效率的方法，即在编译时将函数调用处用函数体替换，类似于 C 语言中的宏展开。这种在函数调用处直接嵌入函数体的函数称为内联函数（Inline Function）；




<br>特点： 

<br>（1）内联函数的一个缺点是程序的整体大小可能会增加；

<br>每一处内联函数的调用都要复制代码，仅仅省去了函数调用的开销，从而提高函数的执行效率；
<br>如果执行函数体内代码的时间，相比于函数调用的开销较大，那么效率的收获会很少；
<br>一般情况，在函数频繁调用且函数内部代码很少的情况下使用内联； 


<br>（2）内联代码替换操作由编译器自行决定。例如，如果某个函数的地址已被占用或编译器判定函数过大，则编译器不会内联该函数；

<br>一个好的编译器将会根据函数的定义体，自动地取消不值得的内联；
<br>对函数作 inline 声明只是程序员对编译器提出的一个建议，而不是强制性的，并非一经指定为 inline 编译器就必须这样做；
<br>编译器有自己的判断能力，它会根据具体情况决定是否这样做。一个好的编译器将会根据函数的定义体，自动地取消不值得的内联；


<br>（3）在类声明中，声明函数而无需&nbsp;inline&nbsp;关键字。&nbsp;inline&nbsp;关键字可以在类声明中指定；结果也相同


<br>注意： 

<br>（1）函数前面加上 inline 关键字，函数并不一定就会展开，inline 展开的具体设计取决于编译器；
<br>（2）即使不使用 inline，函数也不一定就不展开，存在隐式内联。C++ 中在类内定义的所有函数都自动称为内联函数，类的成员函数的定义直接写在类的声明中时，不需要 inline 关键字；
<br>（3）通常，递归函数不应该声明成内联函数，递归调用堆栈的展开并不像循环那么简单，比如递归层数在编译时可能是未知的，大多数编译器都不支持内联递归函数；


<br>示例：
<br>// account.h
class Account
{
public:
    Account(double initial_balance)
    {
        balance = initial_balance;
    }

    double GetBalance() const;
    double Deposit(double amount);
    double Withdraw(double amount);

private:
    double balance;
};

inline double Account::GetBalance() const
{
    return balance;
}

inline double Account::Deposit(double amount)
{
    balance += amount;
    return balance;
}

inline double Account::Withdraw(double amount)
{
    balance -= amount;
    return balance;
}
<br><br>概念：三种方法<br>
<br>方法：编译选项

<br>gcc -O2 test. c -o test，只有在编译器开启优化选项的时候，才会有 inline 行为的存在；
<br>比如对 g++ 在 -O0 时就不会作任何的 inline 处理，对于 -O2 的优化方式，编译器会通过启发式算法决定是否值得对一个函数进行内联，同时要保证不会对生成文件的大小产生较大影响，而 -O3 模式则不在考虑生成文件的大小； 


<br>方法：添加属性

<br>static inline __attribute__((always_inline)) int add(int a,int b)


<br>方法：添加宏

<br>#pragma auto_inline(on/off)；
<br>当使用 #pragma auto_inline(off) 指令时，会关闭对函数的 inline 处理，这时即使在函数前面加了 inline 指令，也不会对函数进行内联处理；


<br><br>代码：隐式内联<br>
<br>C++ 中在类内定义的所有函数都自动称为内联函数（隐式内联）；
<br>#include &lt;stdio.h&gt;
 
class Trace{
public:
	Trace()
	{
		noisy = 0;
	}
	void print(char *s)
	{
		if (noisy)
		{
			printf("%s", s);
		}
	}
	void on(){ noisy = 1; }
	void off(){ noisy = 0; }
private:
	int noisy;
};
<br>代码：显式内联<br>#include &lt;stdio.h&gt;
 
class Trace{
public:
	Trace()
	{
		noisy = 0;
	}
	void print(char *s); //类内没有显示声明
	void on(){ noisy = 1; }
	void off(){ noisy = 0; }
private:
	int noisy;
};
// 类外显示定义
inline void Trace::print(char *s)
{
	if (noisy)
	{
		printf("%s", s);
	}
}
<br><br>概念：内联函数原理<br>
<br>引入： 

<br>第一种报错： C/C++ 语法中，如果变量、函数在同一个工程中被多次定义，链接期间会报类似 “对 xxx 多重定义” 的错误；  
<br>第二种报错：内联函数的声明和定义分别在头文件和源文件中，并且在其他文件中被调用时，链接期间编译器会报 “对 xxx 未定义的引用” 错误。内联函数如果会在多处被调用，则需要将函数的定义写在头文件中；
<br>假设 inline 关键字修饰的函数定义在头文件，并且函数被多次定义，但编译器不会报 “对 xxx 多重定义” 的错误；
<br>之所以不会产生第一种或者第二种错误，是因为编译器对被 inline 修饰的函数做了特殊处理，inline 起到了内联的作用，而其原因式因为 inline 是一个弱符号；


<br>概念：弱符号与强符号

<br>在 C 语言中，编译器默认函数和初始化了的全局变量为强符号（Strong Symbol），未初始化的全局变量为弱符号（Weak Symbol）；
<br>强符号之所以强，是因为它们拥有确切的数据，变量有值，函数有函数体；
<br>弱符号之所以弱，是因为它们还未被初始化，没有确切的数据；


<br>概念：弱符号与强符号的处理

<br>链接器会按照如下的规则处理被多次定义的强符号和弱符号：
<br>

<br>不允许强符号被多次定义，也即不同的目标文件中不能有同名的强符号；如果有多个强符号，那么链接器会报符号重复定义错误；


<br>

<br>如果一个符号在某个目标文件中是强符号，在其他文件中是弱符号，那么选择强符号；


<br>

<br>如果一个符号在所有的目标文件中都是弱符号，那么选择其中占用空间最大的一个；




<br>分析：内联函数作为弱符号<br>
<br>概念：

<br>由于内联函数是弱符号，因此如果多个重定义的函数的符号、在所有的目标文件中都是弱符号，那么选择其中占用空间最大的一个；
<br>并且因为 inline 的弱符号特性避免了重定义，但是也会出现其他坑，比如编译器优化开关关闭时，inline 函数可能会被同名的其它函数替代，弱符号被强符号代替，从而影响业务逻辑的正确性；


<br>示例：InlineMain.cpp
<br>/* InlineMain.cpp */
#include &lt;cstdio&gt;
 
inline void Function()
{
    printf("[Function]========= Get!!!\n");
}
<br>
<br>示例：FunctionDef.cpp
<br>/* FunctionDef.cpp */
#include &lt;cstdio&gt;
 
void Function()
{
    printf("[Another Function]========= Get!!!\n");
}
<br>
<br>示例：
<br>int main() {
    Function();
}
<br>
<br>分析：

<br>以上代码有两种运行结果；
<br>编译器优化开关打开时，运行结果正常不存在问题：[Function]========= Get!!!
<br>编译器优化未打开，运行结果错误：[Another Function]========= Get!!!


]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-4：内联函数.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 4：内联函数.md</guid><pubDate>Thu, 19 Mar 2026 11:31:13 GMT</pubDate></item><item><title><![CDATA[Lecture 5：常量、枚举与union]]></title><description><![CDATA[ 
 <br><br><br>概念：const 和 enum 基础概念<br>
<br>概念：const

<br>核心

<br>在编译器的视角下，define 不被视为 C++ 语言的一部分，因此可能存在在编译器的观测角度，代码的行为与编译器行为不一致的情况，此时可以改用 const 来替代宏；


<br>概念：两类特殊使用情况

<br>常量指针

<br>在编写常量指针时，有必要编写两个 const：const char* const name = "Mike"


<br>class 专属常量

<br>创建专属于某一个 class 的常量：static const int Number = 5;






<br>概念：enum

<br>核心

<br>一个属于枚举类型的数值可以权充 ints 使用（enum hack）


<br>举例

<br>enum { NumTurns = 5};
<br>int scores[NumTurns];


<br>分析

<br>

<br>enum 的行为更类似于 define 而不是 const，如果这是你想要的；


<br>

<br>对 enum 取地址是不合法的，而对 const 取地址是合法的。而取一个 define 的地址也不合法；


<br>

<br>enum 和 define 一样，不会非必要的内存分配；






<br><br>分析：尽可能使用 const<br>
<br>概念： 

<br>

<br>将某些东西声明为 const 可帮助编译器侦测出错误用法，const 可被施加于任何作用域内的对象、函数参数、函数返回类型、成员函数本体；


<br>

<br>编译器强制实施 bitwise constness ，但你在编写程序时应该使用 conceptual consrness (概念上的常量性)；


<br>

<br>当 const 和 non-const 成员函数有着实质上等价的实现时，令 non-const 版本调用 const 可以减少代码重复；




<br>使用：

<br>const 指针

<br>被指示物常量

<br>MyClass const* myClass;
<br>如果 const 出现在星号左边，表示被指示物是常量；


<br>指针常量

<br>MyClass *const myClass;
<br>如果 const 出现在星号右边，表示指针自身为常量；


<br>总结：

<br>使用 const 时，* 在后，不能改值，* 在前，不能改指针；




<br>const 函数

<br>bitwise constness

<br>成员函数只有在不更改对象的任何成员变量（static 除外）时才可以说是 const 的；
<br>即不更改对象内的任何一个 bit；
<br>问题：可以通过在外部重新声明指针指向此值，修改此内容；


<br>logical contness

<br>一个 const 成员函数可以修改它所处理的对象内的某些 bits，但只有在客户端无法侦测出的情况下；




<br>const 转型

<br>就一般的守则而言，转型是一个糟糕的想法；
<br>但是允许 non-const 版本调用 const 可以有效减少代码重复；
<br>相反的，不应该使用 non-const 版本调用 const 内容；




<br><br><br>概念：union<br>
<br>概念： 

<br>union&nbsp;是一个用户定义类型，其中所有成员都共享同一个内存位置；
<br>此定义意味着在任何给定时间，union 都不能包含来自其成员列表的多个对象。这还意味着，无论 union 具有多少成员，它始终仅使用足以存储最大成员的内存；
<br>如果具有大量对象但内存有限，union 可用于节省内存；
<br>但是，需要格外小心才能正确使用 union。你负责确保始终访问分配的同一成员，如果任何成员类型具有不常用构造函数，则必须编写代码来显式构造和销毁该成员。使用 union 之前，应考虑是否可以使用基 class 和派生的 class 类型来更好地表示尝试解决的问题；


<br>语法： 

<br>union&nbsp;tag&nbsp;opt{member-list};


<br>示例：
<br>// declaring_a_union.cpp
union RecordType  
{
    char   ch;
    int    i;
    long   l;
    float  f;
    double d;
    int *int_ptr;
};

int main()
{
    RecordType t;
    t.i = 5; // t holds an int
    t.f = 7.25; // t now holds a float
}
<br><br>概念：使用枚举区分 union 中类型<br>
<br>概念： 

<br>任何访问 union 的代码都需要了解保存数据的成员。解决此问题的最常见解决方案被称为 “可区分 union”；
<br>它将 union 括在 struct 中，还加入一个 enum 成员，用于指示当前存储在 union 中的成员类型；


<br>代码：
<br>#include &lt;queue&gt;

using namespace std;

enum class WeatherDataType
{
    Temperature, Wind
};

struct TempData
{
    int StationId;
    time_t time;
    double current;
    double max;
    double min;
};

struct WindData
{
    int StationId;
    time_t time;
    int speed;
    short direction;
};

// 使用一个枚举来区分当前Union存储的类型
struct Input
{
    WeatherDataType type;
    union
    {
        TempData temp;
        WindData wind;
    };
};

// 给定参数类型的函数
void Process_Temp(TempData t) {}
void Process_Wind(WindData w) {}

void Initialize(std::queue&lt;Input&gt;&amp; inputs)
{
    Input first;
    first.type = WeatherDataType::Temperature;
    first.temp = { 101, 1418855664, 91.8, 108.5, 67.2 };
    inputs.push(first);

    Input second;
    second.type = WeatherDataType::Wind;
    second.wind = { 204, 1418859354, 14, 27 };
    inputs.push(second);
}

// 使用演示
int main(int argc, char* argv[])
{
    // Container for all the data records
    queue&lt;Input&gt; inputs;
    Initialize(inputs);
    
    while (!inputs.empty())
    {
        Input const i = inputs.front();
        
        // 利用enum来得到当前Union的类型，传入switch、进行不同的处理
        // 对存储在Union的不同类型结构体进行不同处理
        switch (i.type)
        {
        case WeatherDataType::Temperature:
            Process_Temp(i.temp);
            break;
        case WeatherDataType::Wind:
            Process_Wind(i.wind);
            break;
        default:
            break;
        }
        inputs.pop();

    }
    return 0;
}
]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-5：常量、枚举与union.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 5：常量、枚举与union.md</guid><pubDate>Thu, 19 Mar 2026 11:37:24 GMT</pubDate></item><item><title><![CDATA[Lecture 6：模板]]></title><description><![CDATA[ 
 <br><br><br>概念：泛型编程<br>
<br>概念： 

<br>泛型编程是指独立与任何类型的方式编写代码。泛型编程和面向对象编程，都依赖与某种形式的多态；
<br>面向对象编程的多态性在运行时应用于存在继承关系的类，一段代码可以可以忽略基类和派生类之间的差异；
<br>在泛型编程中，编写的代码可以用作多种类型的对象；
<br>面向对象编程所依赖的多态性称为运行时多态性，泛型编程所依赖的多态性称为编译时多态性或参数式多态性；


<br>关系： 

<br>模板是泛型编程的基础。模板是创建类或者函数的公式；


<br><br>概念：模板<br>
<br>概念：模板的声明

<br>模板函数：template &lt;typename T&gt; int compare (T t1, T t2);
<br>模板类：template &lt;typename T&gt; class compare;


<br>概念：什么是模板

<br>模板定义并不是真正的定义了一个函数或者类，而是编译器根据程序员缩写的模板和形参来自己写出一个对应版本的定义，这个过程叫做模板实例化；
<br>编译器生成的版本通常被称为模板的实例，编译器为程序员生成对应版本的具体过程，类似宏替换；
<br>模板类在没有调用之前是不会生成代码的，由于编译器并不会直接编译模板本身，所以模板的定义通常放在头文件中；
<br>正是由于模板的出现，才使用 STL 这样的泛型库的实现成为可能；


<br>特点：

<br>（1）模板就是占位符，不需要字符拼接，和普通的代码别无二致，仅仅多了一项模板参数声明；
<br>（2）报错和调试都能准确的指向模板定义的位置，而不是模板实例化的位置；
<br>（3）支持模板参数自动推导，不需要显式写出类型参数，同时也支持显式指定类型参数；
<br>（4）支持隐式实例化 (implicit instantiation)，即由编译器自动实例化使用到的函数。也支持显式实例化 (explicit instantiation)，即手动实例化；


<br>示例：模板函数的定义
<br>template &lt;typename T&gt;
int compare(T &amp; t1, T &amp; t2)
{
    if(t1 &gt; t2) 
        return 1;
    if(t1 == t2)
        return 0;
    if(t1 &lt; t2)
        return -1;
}
<br>
<br>示例：模板类的定义
<br>template &lt;typename T&gt;
class compare
{
private:
    T _val;
public:
    explicit compare(T &amp; val) : _val(val) { }
    explicit compare(T &amp;&amp; val) : _val(val) { }
    bool operator==(T &amp; t)
    {
        return _val == t;
    }
};
<br><br>概念：模板实例化<br>
<br>概念：隐式实例化

<br>C++ 模板（函数模板 / 类模板）本身不是可执行代码，只是编译器生成具体函数 / 类的 “蓝图”；
<br>实例化就是编译器根据这份蓝图，结合具体的类型/值，生成可执行的具体代码的过程；
<br>隐式和显式实例化的核心区别在于：实例化是编译器自动触发，还是程序员手动强制触发；


<br>特点：隐式实例化

<br>（1）编译器只生成程序实际用到的模板实例，未用到的模板分支 / 类型不会生成代码；
<br>（2）实例化发生在编译阶段，而非运行阶段；


<br>示例：隐式实例化
<br>#include &lt;iostream&gt;
using namespace std;

// 模板蓝图：未生成任何可执行代码
template &lt;typename T&gt;
T add(T a, T b) {
    return a + b;
}

int main() {
    // 调用add(1,2)：触发隐式实例化，编译器生成 int add(int, int)
    cout &lt;&lt; add(1, 2) &lt;&lt; endl; 
    // 调用add(1.5, 2.5)：触发隐式实例化，编译器生成 double add(double, double)
    cout &lt;&lt; add(1.5, 2.5) &lt;&lt; endl; 
    return 0;
}
<br>
<br>概念：显式实例化

<br>程序员通过特定语法，强制编译器为指定的类型/值生成模板实例，无论程序是否实际使用该实例；
<br>显式实例化是 “主动触发”，而非编译器 “被动响应”；
<br>显式实例化主要解决模板分离编译的问题（模板声明和定义分离时的编译错误），也可用于提前生成实例、减少重复编译开销；
<br>比如：模板的声明和定义分离是 C++ 的常见写法（声明在 .h，定义在 .cpp），但隐式实例化会报错，原因是编译器编译使用模板的文件时，找不到模板的定义（只有声明），无法完成隐式实例化；


<br>示例：错误的情况
<br>// my_template.h
template &lt;typename T&gt; T add(T a, T b); // 仅声明，无定义

// my_template.cpp
#include "my_template.h"
template &lt;typename T&gt;
T add(T a, T b) { // 模板定义
    return a + b;
}

// main.cpp
#include "my_template.h"
int main() {
    add(1, 2); // 隐式实例化失败：编译器找不到add&lt;int&gt;的定义
    return 0;
}
<br>
<br>示例：显式实例化
<br>// my_template.cpp
#include "my_template.h"
template &lt;typename T&gt;
T add(T a, T b) {
    return a + b;
}

// 显式实例化：强制生成int版本的add
template int add&lt;int&gt;(int, int);
// 若需要double版本，再添加：template double add&lt;double&gt;(double, double);
<br><br><br>分析：普通模板函数<br>
<br>示例：

<br>普通模板函数


<br>分析：

<br>对于支持 operator== 和 operator&gt; 操作的类型，包括基本的 int，float，double 等类型，是完全没有问题的，但是它不能用来比较字符串 (char*)，因为这个函数比较的是串指针，而不是字符串本身；


<br>代码：
<br>template&lt;typename T&gt;
int compare(T a, T b)
{
	cout &lt;&lt; "T" &lt;&lt; endl;
	return a == b ? 0 : (a &gt; b ? 1 : -1);
}
<br><br>分析：模板特化<br>
<br>示例： 

<br>上述模板的特化版本；


<br>分析： 

<br>下述特化的函数能正确比较字符串，由于特化版本和普通版本的函数是可以实现重载的，并且优先匹配普通函数；


<br>代码：
<br>template&lt;typename T&gt;
int compare(T a, T b)
{
	cout &lt;&lt; "T" &lt;&lt; endl;
	return a == b ? 0 : (a &gt; b ? 1 : -1);
}

template&lt;&gt;	//必须写，不然就是重载函数，而不是函数模板，特化版本了
int compare(const char* str1, const char* str2)
{
	cout &lt;&lt; "const char *" &lt;&lt; endl;
	return strcmp(str1, str2);
}
<br>
<br>问题： 

<br>那么将这个特化放在何处？显然是要放在模板的头文件中。但这样会导致符号多重定义的错误。原因很明显，模板特化是一个函数，而非模板；


<br>解决：

<br>函数模板特化即函数，而非模板的概念，完全与普通函数一样；加上 inline 关键字或者分文件实现都是可以的； 
<br>因为编译器直接扩展内联函数，不产生外部符号，在多个模块中 #include 它们没有什么问题。链接器不会出错，因为不存在多重定义的符号。对于像 compare 这样的小函数来说，inline 怎么说都是你想要的（它更快）；


<br>代码：
<br>template&lt;&gt;
inline int compare(const char* str1, const char* str2)
{
	cout &lt;&lt; "特化 const char *" &lt;&lt; endl;
	return strcmp(str1, str2);
}
<br><br><br><br>概念：Concept<br>
<br>引入：

<br>在没有引入 Concept 检查之前，需要在函数内使用假言实现类型检查：
<br>template &lt;typename T&gt; T inc(T&amp; a) { static_assert(std::is_integral&lt;T&gt;::value);return ++a;}
<br>虽然可以解决问题，但是 T 的检查是在函数内实现的，调用时出错了，用户才知道。不能从函数 inc 的声明直观看出 T 的要求。而使用 concept 可以直接报错；


<br>概念： 

<br>C++20 引入了 concept 以在编译期检查模板实参是否满足指定的约束；


<br>示例： 

<br>对整型的类型限制：

<br>template &lt;typename T&gt; concept integral = std::is_integral&lt;T&gt;:: value;


<br>使用： 

<br>template &lt;integral T&gt; T inc(T&amp; a) { return ++a; }




]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-6：模板.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 6：模板.md</guid><pubDate>Thu, 19 Mar 2026 12:30:06 GMT</pubDate></item><item><title><![CDATA[Lecture 7：智能指针]]></title><description><![CDATA[ 
 <br><br>概念：为什么需要智能指针<br>
<br>智能指针就是帮我们 C++ 程序员管理动态分配的内存的，它会帮助我们自动释放 new 出来的内存，从而避免内存泄漏； 
<br>比如 new 了一个字符串指针，但是没有 delete 就已经 return 结束函数了，导致内存没有被释放，内存泄露；
<br>使用指针，我们没有释放，就会造成内存泄露。但是我们使用普通的对象却不会；
<br>因此，如果我们分配的动态内存都交由有生命周期的对象来处理，那么在对象过期时，让它的析构函数删除指向的内存就可以避免上述的内存泄露；
<br><br><br>概念：什么是 auto_ptr<br>
<br>概念： 

<br>auto_ptr 是 c++ 定义的智能指针模板，其定义了管理指针的对象，可以将 new 获得（直接或间接）的地址赋给这种对象；
<br>当对象过期时，其析构函数将使用 delete 来释放内存；
<br>智能指针重载了 * 和 -&gt; 运算符， * 返回普通对象，而 -&gt; 返回指针对象；


<br>用法： 

<br>引用头文件：#include &lt;memory&gt;
<br>使用：auto_ptr&lt;类型&gt; 变量名(new 类型)


<br>注意： 

<br>C++11 之后已经废弃了 auto_ptr，转而使用 unique_ptr 代替；


<br>示例：
<br>class Test {
public:
	Test() { cout &lt;&lt; "Test的构造函数..." &lt;&lt; endl; }
	~Test() { cout &lt;&lt; "Test的析构函数..." &lt;&lt; endl; }

	int getDebug() { return this-&gt;debug; }

private:
	int debug = 20;
};

int main(void) 
{
	// 定义智能指针
	auto_ptr&lt;Test&gt; test(new Test);

	// 使用智能指针，和普通的指针一样
	cout &lt;&lt; "test-&gt;debug：" &lt;&lt; test-&gt;getDebug() &lt;&lt; endl;
	cout &lt;&lt; "(*test).debug：" &lt;&lt; (*test).getDebug() &lt;&lt; endl;

	return 0;
} 

<br><br>方法：get() <br>
<br>作用： 

<br>获取智能指针托管的指针地址；
<br>但我们一般不会这样使用，因为都可以直接使用智能指针去操作；


<br>示例：
<br>// 定义智能指针
auto_ptr&lt;Test&gt; test(new Test);

Test *tmp = test.get();		// 获取指针返回
cout &lt;&lt; "tmp-&gt;debug：" &lt;&lt; tmp-&gt;getDebug() &lt;&lt; endl;
<br>方法：release() <br>
<br>作用： 

<br>取消智能指针对动态内存的托管；


<br>示例：
<br>// 定义智能指针
auto_ptr&lt;Test&gt; test(new Test);

Test *tmp2 = test.release();	// 取消智能指针对动态内存的托管
delete tmp2;	// 之前分配的内存需要自己手动释放
<br>方法：reset()<br>
<br>作用： 

<br>重置智能指针托管的内存地址，如果地址不一致，原来的会被析构掉；


<br>示例：
<br>// 定义智能指针
auto_ptr&lt;Test&gt; test(new Test);

test.reset();			// 释放掉智能指针托管的指针内存，并将其置NULL

test.reset(new Test());	// 释放掉智能指针托管的指针内存，并将参数指针取代之
<br><br>概念：auto_ptr 指针复制或者赋值都会改变资源的所有权<br>// auto_ptr 被C++11抛弃的主要原因
auto_ptr&lt;string&gt; p1(new string("I'm Li Ming!"));
auto_ptr&lt;string&gt; p2(new string("I'm age 22."));

cout &lt;&lt; "p1：" &lt;&lt; p1.get() &lt;&lt; endl;
cout &lt;&lt; "p2：" &lt;&lt; p2.get() &lt;&lt; endl;

// p2赋值给p1后，首先p1会先将自己原先托管的指针释放掉，然后接收托管p2所托管的指针，
// 然后p2所托管的指针制NULL，也就是p1托管了p2托管的指针，而p2放弃了托管。
p1 = p2;	
cout &lt;&lt; "p1 = p2 赋值后：" &lt;&lt; endl;
cout &lt;&lt; "p1：" &lt;&lt; p1.get() &lt;&lt; endl;
cout &lt;&lt; "p2：" &lt;&lt; p2.get() &lt;&lt; endl;
<br><br><br>概念：unique_ptr 基础概念<br>
<br>引入： 

<br>auto_ptr 是用于 C++11 之前的智能指针，由于 auto_ptr 基于排他所有权模式，两个指针不能指向同一个资源，复制或赋值都会改变资源的所有权，auto_ptr 主要有三大问题： 
<br>（1）复制和赋值会改变资源的所有权，不符合人的直觉；
<br>（2）在 STL 容器中使用 auto_ptr 存在重大风险，因为容器内的元素必需支持可复制（copy constructable）和可赋值（assignable）；
<br>（3）不支持对象数组的操作；
<br>因此需要使用 unique_ptr 来解决上述问题；


<br>概念： 

<br>C++11 用更严谨的 unique_ptr 取代了 auto_ptr；


<br>特点： 

<br>

<br>基于排他所有权模式：两个指针不能指向同一个资源；


<br>

<br>无法进行左值 unique_ptr 赋值构造，也无法进行左值复制赋值操作，但允许临时右值赋值构造和赋值；


<br>

<br>保存指向某个对象的指针，当它本身离开作用域时会自动释放它指向的对象；


<br>

<br>在容器中保存指针是安全的；




<br>示例：
<br>unique_ptr&lt;string&gt; p1(new string("I'm Li Ming!"));
unique_ptr&lt;string&gt; p2(new string("I'm age 22."));
	
cout &lt;&lt; "p1：" &lt;&lt; p1.get() &lt;&lt; endl;
cout &lt;&lt; "p2：" &lt;&lt; p2.get() &lt;&lt; endl;

// 错误：p1 = p2;					// 禁止左值赋值
// 错误：unique_ptr&lt;string&gt; p3(p2);	// 禁止左值赋值构造

unique_ptr&lt;string&gt; p3(std::move(p1)); // 使用p1生成其右值，传入p3的构造函数中
p1 = std::move(p2);	// 使用move把左值转成右值就可以赋值了，效果和auto_ptr赋值一样

cout &lt;&lt; "p1 = p2 赋值后：错误" &lt;&lt; endl;
cout &lt;&lt; "p1：" &lt;&lt; p1.get() &lt;&lt; endl;
cout &lt;&lt; "p2：" &lt;&lt; p2.get() &lt;&lt; endl;

// 会自动调用 delete[] 函数去释放内存
unique_ptr&lt;int[]&gt; array(new int[5]);	// 支持这样定义，即允许临时右值赋值构造
<br><br>概念：auto_ptr 与 unique_ptr 智能指针的内存管理陷阱<br>
<br>分析：以下问题是由于 auto_ptr 与 unique_ptr 的排他性所导致的；
<br>auto_ptr&lt;string&gt; p1;
string *str = new string("智能指针的内存管理陷阱");
p1.reset(str);	// p1托管str指针
{
	auto_ptr&lt;string&gt; p2;
	p2.reset(str);	// p2接管str指针时，会先取消p1的托管，然后再对str的托管
}

// 此时p1已经没有托管内容指针了，为NULL，在使用它就会内存报错！
cout &lt;&lt; "str：" &lt;&lt; *p1 &lt;&lt; endl;
<br>
<br>分析：独占所有权导致无法拷贝，仅支持移动

<br>unique_ptr 的核心设计是 “独占对象所有权”，因此它禁用了拷贝构造和拷贝赋值运算符，只能通过 std::move 转移所有权。这是设计特性，但也导致它无法用于需要 “拷贝” 的场景，新手易因误拷贝触发编译错误；
<br>比如：下面代码当中的 std::move(p1) 只是 “标记” p1 的所有权可转移，转移后 p1 变为 “空指针”，若误操作 p1 会触发未定义行为；
<br>因此，无法将 unique_ptr 直接存入需要拷贝的容器 / 函数参数，必须显式移动，增加了使用成本； 


<br>示例：
<br>#include &lt;iostream&gt;
#include &lt;memory&gt;
using namespace std;

class Test {
public:
    Test() { cout &lt;&lt; "Test构造" &lt;&lt; endl; }
    ~Test() { cout &lt;&lt; "Test析构" &lt;&lt; endl; }
};

int main() {
    unique_ptr&lt;Test&gt; p1(new Test);
    
    // 错误1：直接拷贝unique_ptr（编译失败）
    unique_ptr&lt;Test&gt; p2 = p1; // 编译报错：拷贝构造函数被删除
    
    // 错误2：函数参数传递时的隐式拷贝
    auto func = [](unique_ptr&lt;Test&gt; p) {};
    func(p1); // 编译报错：无法拷贝unique_ptr
    
    // 正确：必须用std::move转移所有权
    unique_ptr&lt;Test&gt; p3 = move(p1); 
    func(move(p3)); // 转移后p3失效
    
    // 注意：p1已失效，解引用会导致未定义行为
    // cout &lt;&lt; p1.get() &lt;&lt; endl; // 输出nullptr（多数编译器），但操作p1指向的对象会崩溃
    return 0;
}
<br>
<br>分析：指向数组的语法陷阱

<br>unique_ptr 对 “单个对象” 和 “数组” 的处理是分离的：
<br>（1）unique_ptr&lt;T&gt;：析构时调用 delete（针对单个对象）
<br>（2）unique_ptr&lt;T[]&gt;：析构时调用 delete[]（针对数组）
<br>，用 unique_ptr&lt;T&gt; 指向数组，导致析构时仅释放第一个元素，引发内存泄漏；


<br>示例：
<br>#include &lt;iostream&gt;
#include &lt;memory&gt;
using namespace std;

int main() {
    // 错误写法：用unique_ptr&lt;int&gt;指向数组（析构调用delete，而非delete[]）
    unique_ptr&lt;int&gt; arr_err(new int[5]{1,2,3,4,5}); 
    // 析构时仅释放第一个int，剩余4个int内存泄漏（未定义行为）
    
    // 正确写法：用unique_ptr&lt;int[]&gt;指向数组
    unique_ptr&lt;int[]&gt; arr_ok(new int[5]{1,2,3,4,5});
    // 析构时调用delete[]，正确释放整个数组
    
    return 0;
}
<br><br><br>概念：shared_ptr 基础概念<br>
<br>原理： 

<br>记录引用特定内存对象的智能指针数量，当复制或拷贝时，引用计数加 1，当智能指针析构时，引用计数减 1，如果计数为零，代表已经没有指针指向这块内存，那么我们就释放它。这就是 shared_ptr 采用的策略；


<br>实现：

<br>每个 shared_ptr&lt;T&gt; 对象本身只包含两个指针，64 位系统下共 16 字节，32 位下 8 字节；
<br>（1）T* ptr：指向被管理的实际对象（裸指针）
<br>（2）ControlBlock* cb：指向控制块（核心，存储引用计数、删除器等）
<br>shared_ptr 本身不存储引用计数，所有共享同一对象的 shared_ptr，其 cb 指针都指向同一个控制块，这是实现 “共享计数” 的核心；


<br>概念：控制块

<br>控制块是 shared_ptr 底层最核心的结构，由编译器自动创建，存储所有管理对象所需的元数据；
<br>（1）std::size_t use_count 强引用计数：记录当前有多少 shared_ptr 指向该对象（独占所有权的计数）
<br>（2）std::size_t weak_count 弱引用计数：记录当前有多少 weak_ptr 指向该控制块（仅观察，不持有所有权）
<br>（3）T* object_ptr：指向被管理对象的裸指针（与 shared_ptr 的 ptr 一致，或为 nullptr）
<br>（4）Deleter deleter 自定义删除器：默认是 delete/delete[]，支持用户自定义销毁逻辑；
<br>（5）Allocator allocator 内存分配器：用于释放控制块自身内存；
<br>控制块的类型是 “类型擦除” 的，无论删除器 / 分配器是什么类型，控制块都能统一管理，因此 shared_ptr&lt;void&gt; 也能正确释放任意类型的对象（删除器知道具体类型）


<br>示例： 
<br>class Person {
public:
	Person(int v) {
		this-&gt;no = v;
		cout &lt;&lt; "构造函数 \t no = " &lt;&lt; this-&gt;no &lt;&lt; endl;
	}

	~Person() {
		cout &lt;&lt; "析构函数 \t no = " &lt;&lt; this-&gt;no &lt;&lt; endl;
	}

private:
	int no;
};

// 仿函数，内存删除
class DestructPerson {
public:
	void operator() (Person *pt) {
		cout &lt;&lt; "DestructPerson..." &lt;&lt; endl;
		delete pt;
	}
};
<br>
<br>注意：

<br>绝对不要用同一个裸指针创建多个独立的 shared_ptr，会创建多个控制块，导致重复释放；
<br>
Test* raw = new Test();
shared_ptr&lt;Test&gt; p1(raw); // 创建控制块1，use_count=1
shared_ptr&lt;Test&gt; p2(raw); // 创建控制块2，use_count=1 → 析构时p1、p2各自释放raw，触发double free崩溃




<br><br>概念：引用计数<br>
<br>概念： 

<br>调用 use_count() 函数可以获得当前托管指针的引用计数；


<br>构造： 

<br><img alt="Pasted image 20250114153040.png" src="lib\media\pasted-image-20250114153040.png">


<br>注意： 

<br>（1）使用 shared_ptr 时，一定要注意是否有循环引用。使用 shared_ptr 智能指针时，要注意避免对象交叉使用智能指针的情况，否则会导致内存泄露； 
<br>（2）可以使用 weak_ptr 来避免上述问题；


<br>示例：
<br>shared_ptr&lt;Person&gt; sp1;

shared_ptr&lt;Person&gt; sp2(new Person(2));

// 获取智能指针管控的共享指针的数量	use_count()：引用计数
cout &lt;&lt; "sp1	use_count() = " &lt;&lt; sp1.use_count() &lt;&lt; endl;
cout &lt;&lt; "sp2	use_count() = " &lt;&lt; sp2.use_count() &lt;&lt; endl &lt;&lt; endl;

// 共享
sp1 = sp2;

cout &lt;&lt; "sp1	use_count() = " &lt;&lt; sp1.use_count() &lt;&lt; endl; // 2
cout &lt;&lt; "sp2	use_count() = " &lt;&lt; sp2.use_count() &lt;&lt; endl &lt;&lt; endl; // 2

shared_ptr&lt;Person&gt; sp3(sp1);
cout &lt;&lt; "sp1	use_count() = " &lt;&lt; sp1.use_count() &lt;&lt; endl; // 3
cout &lt;&lt; "sp2	use_count() = " &lt;&lt; sp2.use_count() &lt;&lt; endl; // 3
cout &lt;&lt; "sp2	use_count() = " &lt;&lt; sp3.use_count() &lt;&lt; endl &lt;&lt; endl;  // 3
<br>
<br>分析：

<br>sp1 = sp2; -&gt;sp1 和 sp2 共同托管同一个指针，所以他们的引用计数为 2； 
<br>shared_ptr&lt; Person &gt; sp3(sp1); -&gt; sp1 和 sp2 和 sp3 共同托管同一个指针，所以他们的引用计数为 3； 


<br><br><br>概念：weak_ptr 基础概念<br>
<br>概念：

<br>std::weak_ptr 是 C++11 引入的弱引用智能指针，它并非独立的所有权管理者，而是 std::shared_ptr 的 “辅助工具”，核心作用是观察 shared_ptr 管理的对象，却不持有对象的所有权，因此不会影响对象的生命周期；
<br>其设计初衷是解决 shared_ptr 最典型的问题：循环引用导致的内存泄漏；


<br>区别：
<br><br>
<br>分析：

<br>weak_ptr 完全依赖 shared_ptr 的控制块（Control Block） 实现功能，自身无独立的内存管理；
<br>每个 weak_ptr&lt;T&gt; 对象仅包含一个指针：
<br>weak_ptr&lt;T&gt; { ControlBlock* cb; // 指向 shared_ptr 的控制块（与共享同一对象的 shared_ptr 指向同一个控制块） };
<br>所有观察同一对象的 weak_ptr，其 cb 指针都指向同一个控制块；
<br>因此，weak_ptr 仅操作控制块的 weak_count，不碰 use_count；


<br>补充：

<br>控制块的释放条件：仅当 use_count == 0（对象已释放）且 weak_count == 0（无 weak_ptr 观察）时，才释放控制块内存；


<br><br>概念：weak_ptr 的使用<br>
<br>概念：

<br>weak_ptr 提供的成员函数不多，核心围绕 “检查对象存活” 和 “安全获取强引用” 展开；


<br>方法：lock()

<br>尝试获取一个指向对象的 shared_ptr（强引用），是唯一能安全操作对象的方式；


<br>语法：
<br>std::shared_ptr&lt;T&gt; sp = wp.lock();
if (sp) { // 先判断是否非空
    sp-&gt;func(); // 安全操作对象
}
<br>
<br>方法：expired() 检查对象是否已销毁
<br>if (wp.expired()) {
    cout &lt;&lt; "对象已销毁" &lt;&lt; endl;
} else {
    auto sp = wp.lock(); // 再获取强引用
}
<br>概念：解决 shared_ptr 循环引用<br>
<br>分析：

<br>shared_ptr 双向引用时，强引用计数会 “互锁”，导致计数永远无法减到 0，对象无法释放（内存泄漏）
<br>weak_ptr 可以替换一侧的 shared_ptr，仅保留弱引用，打破循环；


<br>示例：循环引用的问题
<br>#include &lt;iostream&gt;
#include &lt;memory&gt;
using namespace std;

struct Node {
    shared_ptr&lt;Node&gt; next; // 强引用指向另一个节点
    ~Node() { cout &lt;&lt; "Node析构" &lt;&lt; endl; } // 永远不会执行！
};

int main() 
{
    shared_ptr&lt;Node&gt; n1(new Node);
    shared_ptr&lt;Node&gt; n2(new Node);
    
    n1-&gt;next = n2; // n2的use_count=2
    n2-&gt;next = n1; // n1的use_count=2
    
    // 函数结束时，n1/n2析构：n1的use_count=1，n2的use_count=1
    // 强引用计数未到0，对象无法释放 → 内存泄漏
    return 0;
}
<br>
<br>示例：
<br>#include &lt;iostream&gt;
#include &lt;memory&gt;
using namespace std;

struct Node {
    weak_ptr&lt;Node&gt; next; // 弱引用，不增加强引用计数
    ~Node() { cout &lt;&lt; "Node析构" &lt;&lt; endl; } // 正常执行
};

int main() {
    shared_ptr&lt;Node&gt; n1(new Node);
    shared_ptr&lt;Node&gt; n2(new Node);
    
    n1-&gt;next = n2; // 仅递增n2的weak_count，n2的use_count仍=1
    n2-&gt;next = n1; // 仅递增n1的weak_count，n1的use_count仍=1
    
    // 函数结束时，n1/n2析构：
    // n1的use_count=0 → 释放n1对象；n2的use_count=0 → 释放n2对象
    // 最后weak_count=0，释放控制块
    return 0;
}
]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-7：智能指针.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 7：智能指针.md</guid><pubDate>Thu, 19 Mar 2026 13:24:31 GMT</pubDate><enclosure url="lib\media\pasted-image-20250114153040.png" length="0" type="image/png"/><content:encoded>&lt;figure&gt;&lt;img src=&quot;lib\media\pasted-image-20250114153040.png&quot;&gt;&lt;/figure&gt;</content:encoded></item><item><title><![CDATA[Lecture 8：函数指针]]></title><description><![CDATA[ 
 <br><br>概念：什么是函数指针<br>
<br>概念：

<br>函数指针指向的是函数而非对象；


<br>概念：函数的类型

<br>和其他指针一样，函数指针指向某种特定类型；
<br>函数的类型由它的返回类型和形参类型共同决定，与函数名无关；
<br>比如：bool test(const string&amp;)
<br>其类型为 bool(const string&amp;)；


<br>方法：声明指向函数的指针

<br>使用：

<br>用指针替换函数名即可，比如：
<br>bool (*pf)(const string &amp;);


<br>解释：

<br>pf 前面有个 *，因此 pf 是指针；
<br>右侧是形参列表，表示 pf 指向的是函数；
<br>再观察左侧，函数的返回类型是布尔值；
<br>因此，p 就是一个指向函数的指针，其中该函数的参数是一个 const string 的引用，返回值是 bool 的类型；


<br>注意：

<br>*pf 两端的括号必不可少；
<br>如果不写这对括号，则 pf 是一个返回值为 bool 指针的函数；




<br>方法：使用函数指针

<br>可以将函数名作为一个值使用，此时该函数自动转化为指针；

<br>pf = test;
<br>或者是 pf = &amp;test;，这个和 pf = test 等价；


<br>调用指向函数的指针：

<br>bool b1 = test("hello");
<br>等价于：bool b1 = pf("hello");




<br><br>概念：重载函数的指针<br>
<br>概念：

<br>当我们使用重载函数时，上下文必须清晰地界定到底应该选用哪个函数；


<br>分析：如果定义了指向重载函数的指针

<br>void ff(int*);
<br>void ff(unsigned int);


<br>分析：

<br>编译器通过指针类型决定选用哪个函数，指针类型必须与重载函数中的某一个精确匹配；
<br>比如：

<br>正确：void (*pf1)(unsigned int) = ff; 
<br>正确：void (*pf2)(int*) = ff;
<br>错误：double (*pf3)(int*) = ff;




<br><br>概念：函数指针形参<br>
<br>概念:

<br>虽然不能定义函数类型的形参，但是形参可以是指向函数的指针；
<br>此时，形参看起来是函数类型，实际上却是当成指针使用；


<br>使用：

<br>情况一：形参作为函数类型，自动转化为指向函数的指针

<br>void useBigger(const string &amp;s1,const string &amp;s2,bool pf(const string&amp;,const string&amp;));


<br>情况二：显示的将形参定义为指向函数的指针

<br>void useBigger(const string &amp;s1,const string &amp;s2,bool (*pf)(const string&amp;,const string&amp;))


<br>使用时：

<br>useBigger("Hello","World",testFun);
<br>这里会自动将函数 testFun() 转化为指向该函数的指针；




<br><br>概念：返回指向函数的指针<br>
<br>概念：

<br>虽然不能返回一个函数，但是能返回指向函数类型的指针；
<br>然而，我们必须把返回类型写成指针形式，编译器不会自动地将函数返回类型当成对应的指针类型处理；


<br>使用：基于类型别名

<br>要想声明一个返回函数指针的函数，最简单的办法是使用类型别名；

<br>using F = int(int*,int); 
<br>此时 F 为函数类型，而非指针；


<br>此时可以使用类型别名，将 F 定义为"返回指向函数的指针"的类型：

<br>F *f1(int);




]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-8：函数指针.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 8：函数指针.md</guid><pubDate>Mon, 28 Apr 2025 09:15:59 GMT</pubDate></item><item><title><![CDATA[Lecture 9：友元]]></title><description><![CDATA[ 
 <br><br>概念：什么是友元<br>
<br>概念：

<br>类可以允许其他类或者函数访问它的非公有成员，方法是令其他类或者函数成为它的友元 (friend)；
<br>如果类想把一个函数作为它的友元，只需要增加一条以 friend 关键字开始的函数声明语句即可；
<br>友元声明只能出现在类定义的内部，但是在类内出现的具体位置不限；
<br>友元不是类的成员，也不受它所在区域访问控制级别的约束；


<br>注意：

<br>一般来说，最好在类定义开始或结束前的位置集中声明友元；


<br>示例：
<br>class Data
{
	// 为 Sales_data 的非成员函数所做的友元声明
	friend void add(const int,const int);
}

// 非成员组成部分的声明
void add(const int);
<br><br>概念：友元的声明与使用<br>
<br>概念：

<br>友元的声明仅仅指定了访问的权限，而非一个通常意义上的函数声明；
<br>如果我们希望类的用户能够调用某个友元函数，那么我们就必须在友元声明之外，再专门对函数进行一次声明；


<br>使用：

<br>为了使友元对类的用户可见，我们通常把友元的声明与类本身放置在同一个头文件中 (类的外部)；


]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-9：友元.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 9：友元.md</guid><pubDate>Thu, 19 Mar 2026 13:27:15 GMT</pubDate></item><item><title><![CDATA[Lecture 10：表达式]]></title><description><![CDATA[ 
 <br><br>概念：成员访问表达式<br>
<br>概念：点运算符

<br>作用：用于获取类对象的一个成员；


<br>概念：箭头运算符

<br>作用：表示对变量先解引用，然后再获取类对象的一个成员；

<br>即：ptr-&gt;mem 等价于 (*ptr).mem；




<br>注意：解引用运算符的优先级低于点运算符
<br>示例：
<br>string s1 = "a string";
string* p = &amp;s1; // 对s1变量取地址
auto n = s1.size();
n = (*p).size(); // 和s1.size()效果一致
n = p-&gt;size(); // 和s1.size()效果一致
<br><br>概念：位运算符<br>
<br>概念：

<br>位运算符作用于整数类型的运算对象，并把运算对象看成是二进制位的集合；


<br>图示：

<br><img alt="Pasted image 20250418143052.png" src="lib\media\pasted-image-20250418143052.png">


<br>注意：

<br>关于符号位如何处理没有明确的规定，所以强烈建议仅将位运算符用于处理无符号类型；


<br><br>概念：移位运算符<br>
<br>概念：左移运算符

<br>左移运算符 (&lt;&lt;) 在右侧插入值为 0 的二进制位；
<br>示例：

<br><img alt="Pasted image 20250418143404.png" src="lib\media\pasted-image-20250418143404.png">




<br>概念：右移运算符

<br>右移运算符 (&gt;&gt;) 的行为则依赖于其左侧运算对象的类型；
<br>如果该运算对象是无符号类型, 在左侧插入值为 0 的二进制位；
<br>如果该运算对象是带符号类型，在左侧插入符号位的副本或值为 0 的二进制位，如何选择要视具体环境而定；
<br>示例：

<br><img alt="Pasted image 20250418143456.png" src="lib\media\pasted-image-20250418143456.png">




<br>分析：移位运算符满足左结合律

<br>举例： cout&lt;&lt;"hi"&lt;&lt;"there"&lt;&lt;endl;
<br>等价于：(((cout&lt;&lt;"hi")&lt;&lt;"there")&lt;&lt;endl;
<br>在这条语句中，运算对象"hi"和第一个 &lt;&lt; 组合在一起, 它的结果和第二个 &lt;&lt; 组合在一起接下来的结果再和第三个 &lt;&lt; 组合在一起； 


<br>分析：移位运算符的优先级

<br>移位运算符的优先级不高不低，介于中间；
<br>比算术运算符的优先级低，但比关系运符、赋值运算符和条件运算符的优先级高；
<br>因此，在一次使用多个运算符时，有必要加上括号使其满足我们的要求；


<br>代码：
<br>void operatorTest::Run2()
{
    int number = -1024;
    int numberLeft = number &lt;&lt; 1;
    cout &lt;&lt; numberLeft &lt;&lt; endl;
    int numberLeft2 = number &lt;&lt; 2;
    cout &lt;&lt; numberLeft2 &lt;&lt; endl;
    int numberRight = number &gt;&gt; 1;
    cout &lt;&lt; numberRight &lt;&lt; endl;

    unsigned int number2 = 1024;
    cout &lt;&lt; "number2:" &lt;&lt; (number2 &gt;&gt; 2) &lt;&lt; endl;
}
<br>
<br>输出：

<br><img alt="Pasted image 20250418144040.png" src="lib\media\pasted-image-20250418144040.png">


<br><br>概念：逐位运算<br>
<br>概念：逐位求反

<br>int number3 = -1024;cout &lt;&lt; ~number3 &lt;&lt; endl;
<br>输出：1023


<br>概念：位与、位或、位异或运算符

<br>示例：

<br><img alt="Pasted image 20250418144406.png" src="lib\media\pasted-image-20250418144406.png">


<br>与：&amp;，两个都为 1，则结果为 1，否则为 0；
<br>或：|，至少有一个为 1，则为 1，否则为 0；
<br>异或：^，只有一个 1，则结果 1，否则为 0；


<br><br>概念：sizeof 运算符<br>
<br>概念：

<br>sizeof 运算符返回一条表达式或一个类型名字所占的字节数；
<br>sizeof 运算符满足右结合律，其所得的值是一个 size_t 类型；


<br>分类：

<br>第一类：sizeof expr
<br>第二类：sizeof(type)


<br>示例：
<br>class Test
{
public:
    int i = 123;
    char j = 'c';
    double k = 123.1234;
};

void operatorTest::Run3()
{
    Test testClass, *ptr;
    cout &lt;&lt; "testClass:" &lt;&lt; sizeof(testClass) &lt;&lt; endl;
    cout &lt;&lt; sizeof(ptr) &lt;&lt; endl;
    cout &lt;&lt; sizeof *ptr &lt;&lt; endl;
}
<br>
<br>输出：
<br>testClass:16
8
16
<br>
<br>分析：

<br>第一个 sizeof (testClass) 表示输出 Test 类型的大小；
<br>第二个 sizeof(ptr) 表示输出指针的大小；
<br>第三个 sizeof *ptr 表示输出 ptr 解引用后的类型的大小，即表示输出 Test 类型的大小；


<br>注意：sizeof expr 中的 expr 不会实际进行运算，因此 expr 的结果可以是空，只要其类型正确即可；

<br>在第二种形式中，sizeof 返回的是表达式结果类型的大小；
<br>与众不同的一点是，sizeof 并不实际计算其运算对象的值，所以即使 ptr 是一个无效 (即未初始化) 的指针也不会有什么影响；
<br>在 sizeof 的运算对象中，解引用一个无效指针仍然是一种安全的行为，因为指针实际上并没有被真正使用；
<br>sizeof 不需要真的解引用指针也能知道它所指对象的类型；


]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-10：表达式.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 10：表达式.md</guid><pubDate>Fri, 18 Apr 2025 07:15:12 GMT</pubDate><enclosure url="lib\media\pasted-image-20250418143052.png" length="0" type="image/png"/><content:encoded>&lt;figure&gt;&lt;img src=&quot;lib\media\pasted-image-20250418143052.png&quot;&gt;&lt;/figure&gt;</content:encoded></item><item><title><![CDATA[Lecture 11：异常处理]]></title><description><![CDATA[ 
 <br><br>概念：throw 表达式<br>
<br>概念：

<br>程序的异常检测部分使用 throw 表达式引发一个异常；
<br>throw 表达式包含关键字 throw 和紧随其后的一个表达式，其中表达式的类型就是抛出的异常类型；
<br>throw 表达式后面通常紧跟一个分号，从而构成一条表达式语句；


<br>示例：

<br>throw runtime_error("Test Runtime Error!");
<br>其中，类型 runtime_error 是标准库异常类型的一种，定义在 stdexcept 头文件中；


<br><br>概念：try 语句块<br>
<br>语法：

<br><img alt="Pasted image 20250418152443.png" src="lib\media\pasted-image-20250418152443.png">


<br>概念：

<br>跟在 try 块之后的是一个或多个 catch 子句；
<br>catch 子句包括三部分：

<br>

<br>关键字 catch；


<br>

<br>括号内一个 (可能未命名的) 对象的声明 (称作异常声明，exception declaration)；


<br>

<br>对应的一个块




<br>当选中了某个 catch 子句处理异常之后，执行与之对应的块；
<br>catch 一旦完成，程序跳转到 try 语句块最后一个 catch 子句之后的那条语句继续执行；


<br>分析：函数在寻找处理代码的过程中退出

<br>在复杂系统中，程序在遇到抛出异常的代码前，其执行路径可能已经经过了多个 try 语句块；
<br>例如，一个 try 语句块可能调用了包含另一个 try 语句块的函数，新的 try 语句块可能调用了包含又一个 try 语句块的新函数，以此类推；
<br>寻找处理代码的过程与函数调用链刚好相反：

<br>

<br>当异常被抛出时，首先搜索抛出该异常的函数；


<br>

<br>如果没找到匹配的 catch 子句，终止该函数，并在调用该函数的函数中继续寻找；


<br>

<br>如果还是没有找到匹配的 catch 子句，这个新的函数也被终止，继续搜索调用它的函数；


<br>

<br>以此类推，沿着程序的执行路径逐层回退，直到找到适当类型的 catch 子句为止；


<br>

<br>如果最终还是没能找到任何匹配的 catch 子句，程序转到名为 terminate 的标准库函数；


<br>该函数的行为与系统有关，一般情况下，执行该函数将导致程序非正常退出；
<br>如果一段程序没有 try 语句块且发生了异常，系统会调用 terminate 函数并终止当前程序的执行；






<br><br>概念：标准异常<br>
<br>概念：

<br>C++标准库定义了一组类，用于报告标准库函数遇到的问题；


<br>分类：

<br>这些异常类也可以在用户编写的程序中使用，它们分别定义在 4 个头文件中：
<br>

<br>exception 头文件定义了最通用的异常类 exception；


<br>它只报告异常的发生，不提供任何额外信息；


<br>

<br>stdexcept 头文件定义了几种常用的异常类；


<br><img alt="Pasted image 20250418155355.png" src="lib\media\pasted-image-20250418155355.png">


<br>

<br>new 头文件定义了 bad_alloc 异常类型；


<br>

<br>type_info 头文件定义了 bad_cast 异常类型；




]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-11：异常处理.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 11：异常处理.md</guid><pubDate>Fri, 18 Apr 2025 07:53:57 GMT</pubDate><enclosure url="lib\media\pasted-image-20250418152443.png" length="0" type="image/png"/><content:encoded>&lt;figure&gt;&lt;img src=&quot;lib\media\pasted-image-20250418152443.png&quot;&gt;&lt;/figure&gt;</content:encoded></item><item><title><![CDATA[Lecture 12：默认实参与调试帮助]]></title><description><![CDATA[ 
 <br><br>概念：默认实参<br>
<br>概念：

<br>某些函数有这样一种形参，在函数的很多次调用中它们都被赋予一个相同的值，此时我们把这个反复出现的值称为函数的默认实参 (default argument)；
<br>调用含有默认实参的函数时，可以包含该实参，也可以省略该实参；
<br>当设计含有默认实参的函数时，其中一项任务是合理设置形参的顺序，尽量让不怎么使用默认值的形参出现在前面，而让那些经常使用默认值的形参出现在后面；


<br>注意：

<br>一旦某个形参被赋予了默认值，它后面的所有形参都必须有默认值；


<br>示例：默认实参的使用：

<br>typedef string::size_type sz;
<br>string s1(sz ht=24,sz wid = 80,char back=' ');


<br>概念：默认实参初始化

<br>局部变量不能作为默认实参；
<br>除此之外，只要表达式的类型能转换成形参所需的类型，该表达式就能作为默认实参；


<br>示例：默认实参初始化
<br>class Function

{

public:

    typedef string::size_type sz;

    sz ht();

    sz wd = 64;

    char t = 'w';

    string szeTest(sz = ht(),sz = wd,char = t);

};
<br><br><br>概念：assert 预处理宏<br>
<br>概念：

<br>预处理宏其实是一个预处理变量，它的行为有点类似于内联函数；
<br>首先对 expr 求值，如果表达式为假 (即 0)，assert 输出信息并终止程序的执行；
<br>如果表达式为真 (即非 0)，assert 什么也不做；


<br>注意：

<br>

<br>assert 宏定义在 cassert 头文件中；


<br>

<br>应该使用 assert 而不是 std::assert，也不需要为 assert 提供 using 声明；




<br>分析：assert 宏常用于检查“不能发生”的条件

<br>assert(word.size() &gt; threshold)


<br><br>概念：NDEBUG 预处理变量与 assert<br>
<br>概念：

<br>assert 的行为依赖于一个名为 NDEBUG 的预处理变量的状态；
<br>如果定义了 NDEBUG，则 assert 什么也不做，默认状态下没有定义 NDEBUG，此时 assert 将执行运行时检查；


<br>使用：

<br>我们可以使用一个 #define 定义 NDEBUG ，从而关闭调试状态；


<br>概念：NDEBUG 预处理变量与代码<br>
<br>概念：

<br>除了用于 assert 外，也可以使用 NDEBUG 编写自己的条件调试代码；
<br>如果 NDEBUG 未定义，将执行 #ifndef 和 #endif 之间的代码；
<br>如果定义了 NDEBUG，这些代码将被忽略掉；


<br>举例：
<br>void print()
{
#ifndef NDEBUG

	cout &lt;&lt; __func__ &lt;&lt; ":测试" &lt;&lt; endl;

#endif
}
<br>
<br>分类：常用变量

<br>__FILE__：当前文件名的字符串字面值；
<br>__LINE__：当前行号；
<br>__TIME__：编译时间；
<br>__DATA__：日期
<br>__func__：当前函数名；


<br>举例：
<br>cout&lt;&lt;"Error:"&lt;&lt;__FILE__&lt;&lt;":in function"&lt;&lt;__func__;
]]></description><link>03_计算机工程技能\01_编程语言基础\01_cpp\01_基础语法\lecture-12：默认实参与调试帮助.html</link><guid isPermaLink="false">03_计算机工程技能/01_编程语言基础/01_CPP/01_基础语法/Lecture 12：默认实参与调试帮助.md</guid><pubDate>Sun, 27 Apr 2025 09:34:11 GMT</pubDate></item><item><title><![CDATA[index]]></title><description><![CDATA[ 
 <br>这是我用于学习游戏开发与计算机图形学的笔记仓库，按技术方向分类整理，方便系统梳理知识体系。<br><br>
<br>01_CPP：C++ 语言相关学习笔记
<br>02_CSharp：C# 语言学习笔记，包含语法特性、.脚本编写结合实践。
<br>03_Python：Python 语言学习笔记
<br><br>
<br>01_基础理论：计算机图形学核心基础理论笔记
<br>02_实时渲染：实时渲染技术笔记，涵盖光栅化渲染流程、着色器编程（GLSL/HLSL）、PBR 物理基于渲染、后处理效果等实时渲染管线技术
<br>03_光线追踪：光线追踪技术笔记
<br>04_软光栅渲染器：软光栅渲染器实现笔记，从 0 到 1 实现 CPU 端光栅化渲染器，包含三角形遍历、深度测试、纹理采样等核心模块。
<br>05_渲染管线技术：现代图形渲染管线笔记
<br><br>
<br>01_游戏引擎技术：游戏引擎核心技术笔记
<br>02_游戏数据系统：游戏数据管理与持久化笔记
<br>03_游戏渲染技术：游戏引擎渲染实践笔记
<br>04_热更新技术：游戏热更新技术笔记，包含资源热更、代码热更（如 XLua/ToLua）、补丁分发与版本管理方案。
<br>05_游戏架构技术：游戏项目架构设计笔记，覆盖 MVC/ECS 等架构模式
<br>06_游戏人工智能：游戏 AI 技术笔记，包含有限状态机、寻路算法（A*、NavMesh）等
<br>07_游戏网络技术：游戏网络编程笔记
<br>08_游戏性能优化：游戏性能优化笔记
<br><br>
<br>

<br>左边的文件目录显示错误，需要设置；


<br>

<br>不能调整左右边框；


<br>

<br>去除掉一些不用的功能，比如搜索、交互图等等；


<br>

<br>继续定制 Index 内容，添加背景图片和内容介绍；


]]></description><link>index.html</link><guid isPermaLink="false">index.md</guid><pubDate>Wed, 18 Mar 2026 11:54:58 GMT</pubDate></item></channel></rss>